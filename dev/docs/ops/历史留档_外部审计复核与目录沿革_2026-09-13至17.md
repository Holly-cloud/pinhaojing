# 历史留档 · 外部审计复核 + 目录位置沿革 + 素材引用

> **来源**：2026-09-21 从根 `README.md` 迁入（README 改为「供人类阅读的首页」，历史物证移到此文）。
> **性质**：历史记录（冻结）——内容按当时原样保留，**不作为当前状态依据**。

---

## 一、2026-09-13 外部审计复核（只读，产品未改动一行）

> 来源：Holly 转来的外部审计报告（对象 = PHJ.html **v7.7**）。逐条复核，真机取证脚本见 `dev/_qa/diag/archive/diag_audit_claims.mjs`。
> **环境前提**：该报告在**另一份副本 / 另一台环境**中产出（本机无其引用的证据目录），故报告截图在本机不可复核。

| 审计判定 | 复核结果 |
|---|---|
| P0 文档未入档 v7.7 | **时间差**（其快照早于本 session 补档）；现已补档。 |
| P0 删模板提示 undefined | **确认 · 真 bug**（`deleteTemplate()` 读 v6.13 已废弃的 `t.name`；修法 = 改用序号）。 |
| P0 30/32 脚本路径失效 | **结论不成立**（本机同口径全绿）；**唯一真失效 = `verify_v6.mjs`**（旧目录名）；「硬编码路径」数字属实 → 已改相对解析。 |
| P1 无全局撤销 / 无自动备份 / 双窗口互相覆盖 | 三条**成立**（备份为**单槽**，比报告所述更弱）。 |
| P1 复制拼接首行空行 | **确认 · 真 bug**（首条目带前缀时输出以空行开头）。★注：该「空行」后被判定为 **v6.21 的有意规则**（A2a/A2c/A2d/A2e 四条断言明确要求），修复曾被门禁打回——见 PROJECT_MEMORY「门禁即规格」。 |
| P1 拼接栏遮挡画布右侧 560px | 成立，但属**既有设计取舍**（不建议块坐标自动避让）。 |
| C1–C7（无引导/搜索/触屏、模板不含内容块、编辑器 80vh、色相聚簇、着色范围） | 事实成立，但**均属既有拍板口径**；改动需明确授权。 |
| 性能「200 块 66ms」 | 趋势成立、**数字不可跨机引用**（本机同口径 126.8ms）。 |
| 它表扬的 6 点 | 复核无异议（其中「对比度 ≥4.76:1」实测 4.80:1 ✓、「块宽整行」`overflowPx=0` ✓）。 |

> 本次复核**未执行任何修复**：只做只读核查 + 文档落盘。

---

## 二、目录位置沿革

- **2026-09-09**：目录由用户手动改名为 `D:\Hermes_Store\拼好镜\`。
- **2026-09-13**：整体迁入 `D:\Hermes_Store\AA-Dev\拼好镜\`（交付物 + `dev/` + git 仓库整体搬）。迁移后实测：构建产物 **155315 B 逐字节不变** + 等价性 PASS + 回归 85/85 + F 18/18 + G 16/16 → 「脚本路径相对自身解析」这条迁移前提成立，任意目录可用。
- **2026-09-17**：**项目合集彻底重整**——合集根 `AA_Dev/` 新增入口 README 与仓库外资产区 `_assets/`；`dev/` 文档按语义分类归入 `dev/docs/`；验收目录 `dev/_build/` 更名为 `dev/_qa/`（工装 + 基准 + 物证），生成物隔离到 `dev/_qa/gen/`、留档归入 `dev/_qa/records/`；删除 4 组逐字节重复副本 + 1 个可再生生成物。产物仍 **230492 B 逐字节不变**、闸门 7/7。
- **2026-09-17**：**R2/R3/R4 收口**——`shell/boot.js` 改名 `shell/wiring.js`（R2）；「引擎零领域语义」机制化为**棘轮** + **皮肤可摘除**断言（R3/R4）。产物 230492 → **230494 B**（仅 1 个导出键改名），`verify_v78` 50 → **53**。
- **2026-09-17**：**F1/F2/F3 收尾（并入 v7.14，不新开 v7.15）**——修正 5 处陈旧活引用（纯文件名替换）；R3-B 零容忍集补上**全部 `【…】` 完整标记串**；新增 **R3-C 防真空自检**。产物 230494 → **230515 B**（仅 5 处注释），`verify_v78` 53 → **54**。
- **之后（2026-09-18 ~ 09-20）**：v7.15 引入写作台默认视图与宿主化、v7.16 补全增强、v7.17 逗号转空格、v7.18 多项目、v7.19 重影与写回修复、v7.20 项目名常显与灵感气泡群；并建立铁律区 / 多 agent 协作机制 / 跨平台巡回。逐版详见 `dev/CHANGELOG.md`。

> 历史文档里出现的旧路径（`分镜提示词管理面板`、`C:\Users\Holly\…\拼好镜`、`dev/_build/…`、`dev/01_…`–`dev/11_…` 等）**按沿革原样保留**，不作为当前位置依据；**当前路径一律以 `dev/docs/README.md` 的映射表与根 README 为准**。

---

## 三、素材引用（历史记录）

> ⚠️ 以下为**历史环境**中的外部资料路径，本机已不存在（2026-09-21 核实），仅作沿革保留；
> **项目不依赖任何外部素材运行**（符合铁律 A1④「不绑定机器特定事实」）。

- 分镜结构依据：`D:\Hermes_Store\已封装的资料\Seedance2-提示词资料\00_改写规则-v1.0.md`（八要素 + 时间轴分镜）
- 验收测试数据取材：`D:\Hermes_Store\已封装的资料\Seedance2-提示词资料\01_演示示例集.md`
