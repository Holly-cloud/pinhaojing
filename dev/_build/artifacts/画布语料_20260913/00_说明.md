# 画布语料留档 —— 即梦画布「补补补」（2026-09-13 取）

> 用途：v7.8 编辑器补全功能的**依据语料**。日后 Holly 想「让小汐读一读我的写作风格」「琢磨新创意」时，从这里起步即可，不必再连画布。
> 来源：即梦画布 `补补补`，`projectId = 5a151e56-27e6-4f19-867c-6068dd521385`；**53 个视频节点全文**（Holly 亲手创建与编辑）。
> 取法（只读，未触发生成/未花积分）：`dreamina-canvas node find --project-id <id> --type video --limit 50 --offset N` → 逐节点 `dreamina-canvas node show --project-id <id> --node-id <id>`。

| 文件 | 内容 |
|---|---|
| `corpus.json` | 结构化语料：每条 = {nodeId, title, mode, model, ratio, resolution, durationSeconds, refs, prompt, promptLen, lines} |
| `prompts_dump.txt` | 53 条逐条原文（带节点名/引用/长度抬头，便于人读） |
| `regularities_report.txt` | 规律分析输出（骨架序列 / 镜头句槽位 / 运镜词频 / 台词模板 / 引用位置 / 换行 / 标点） |
| `snippets_phrases.json` | 正文体高频短语 top60（跨节点 ≥3 次） |
| `refs_seed.json` | 正文引用素材清单（id / 次数 / 名称，共 127 项） |
| `shows/` | 53 个节点的 `node show` 原始响应（JSON，逐节点一份）——`refs_seed.mjs` 依赖它 |
| `collect_ids.mjs` `aggregate.mjs` | 取数与汇总 |
| `style_analysis.mjs` `phrase_analysis.mjs` `regularities.mjs` `vocab_gap.mjs` `saving_estimate.mjs` `trigger_scan.mjs` `final_numbers.mjs` `refs_seed.mjs` | 各项分析脚本（**路径为 2026-09-13 采集时的暂存目录，迁移后需改 `dir` 常量**） |
| `recon_editor_space.mjs` | 编辑器左右空白实测探针（1920×1080，量 `.blk-win` 几何 + 出截图） |

**关键数字**（详见 `dev/07_编辑器补全_v7.8.md`）：53 条 / 45,253 字符 / 平均 854 字符；定型件 542 字符占这类 prompt 的 63%（44/53 条共用，归一化后仅 6 个变体）；正文 141 句镜头句（中位 66 字符）；`{{node:}}` 引用 296 处 / 127 个素材 id。
