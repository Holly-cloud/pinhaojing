# 02 工程计划 —— 分镜提示词管理面板（v3(final) 终版）

> 阶段：考察 → 计划 → **v3(final) 已完成（2026-09-07，验收全绿）**，等待用户试用 → M3
> 功能现状以 `00_README.md` 为准；本文件 §4 历史方案中残留的「序号徽章/顺序推导」描述均已随 2026-09-07 22:35「移除序号标记」拍板失效。
> 小汐 ｜ 2026-09-07 ｜ 任务单阶段：判断筹备 + 收集信息（已完成）→ 开工（已完成）
> 权限：Read-Only（工作空间 `D:\Hermes_Store` 可操作；系统改动需逐项审批）
> 配套流程：门禁 + 工件落盘；需求发掘已拍板跳过

---

## 1. 一句话定位

> **分镜提示词管理面板**：一个「双击即开、用完即走」的单文件 HTML 小工具。把一条视频提示词拆成**无限画布上自由摆放的提示词块**：画布无边无滚动条、拖空白平移视角；块可任意重叠且**拖动手柄永远置顶可操作**；**复制以单块为准**（复制 / 克隆 / 删除）；块内为**原生 textarea**（回车 / Backspace 全原生、零代码干预），高度随内容自动长高全文展开；数据自动保存 + 导出/导入 JSON。**零安装、零进程、零系统残留。**

## 2. 已拍板决策

| # | 决策项 | 拍板 |
|---|---|---|
| 1 | 技术形态 | **单文件 HTML**（file:// 直开，全内联，无 CDN，约 20KB） |
| 2 | 块设计 | **极简通用文本块**：无标题、无字段化、无模型预设 |
| 3 | 界面 v2 | **自由画布**：块绝对定位任意摆放 |
| 4 | 界面 v3 | **无限画布**：视口锁定无滚动条、坐标不限（可负）、**拖空白平移**浏览任意区域；pan 持久化 |
| 5 | 把手置顶 | 拖动手柄 `z-index:9999` + 白底浮起，**重叠时始终可见可操作**；hover 整块置顶（z 7000） |
| 6 | 块编辑器 | **原生 textarea**（v3 的 contenteditable 分段改进已于 2026-09-07 22:13 回退）；回车/Backspace 全原生；autoResize 全文展开 |
| 7 | 复制（v3 final） | **移除「复制全部」**（2026-09-07 用户拍板）；复制以单块为准（块上「复制」trim 首尾进剪贴板；「克隆」生成副本） |
| 8 | 顺序语义 | ~~位置推导 + 序号徽章~~ → **徽章已移除（2026-09-07 用户拍板）**；位置仅存于数据（x/y），不做顺序展示 |
| 9 | 拖拽 | 手写 mouse 事件双模式：块把手拖拽 + 空白平移；无第三方库 |
| 10 | 数据模型 | v3：`{app, version:3, title, pan:{x,y}, blocks:[{id,text,x,y}]}`；v1/v2 自动迁移 |
| 11 | 持久化 | localStorage 防抖保存（400ms）+ beforeunload/visibilitychange 兜底 + 导出/导入 JSON（导入前备份可撤销） |

## 3. 需求梳理（轻量 PRD 骨架）

### 3.1 用户与场景
- **用户**：本人。写视频模型分镜提示词（Seedance / H3 / 其它）时，像在无限便签画布上摊开「一段段提示词」，自由摆位、任意重叠比照。
- **场景**：写分镜 → 摆位（徽章提示相对顺序）→ 对需要的块单块复制去平台 / 克隆复用 → 关掉浏览器，不留痕。

### 3.2 功能分级
| 级别 | 功能 | 说明 |
|---|---|---|
| **P0** | 提示词块增/删/编辑/克隆 | 块 = 原生 textarea（多行，回车/Backspace 全原生） |
| **P0** | 无限画布 | 无滚动条、坐标不限；拖空白平移；pan 持久化 |
| **P0** | 把手置顶 | 重叠时把手永远可见可操作；hover 整块置顶 |
| **P0** | 单块复制 | 块上「复制」→ 该块文本（trim 首尾）进剪贴板；「克隆」→ 副本错位 +(28,28) |
| **P0** | 自动保存 | 编辑/移动/平移后防抖落盘，刷新/重开不丢 |
| **P0** | 导出/导入 JSON | 含位置与视角；导入前自动备份、可撤销 |
| **P0** | 零污染运行 | 双击即用；无安装/服务/环境变量/系统目录写入 |
| **P1** | 旧数据迁移 | v1（列表+标题）/ v2（画布无 pan）自动迁移为 v3 |
| **P1** | 剪贴板兜底 | Firefox file:// 下 execCommand 兜底 |
| **P2 / 明确不做** | 复制全部（已移除）/ 模型预设 / 字段化表单 / 多项目 / 折叠 / 画布缩放 / 主题皮肤 | 超出边界 |

### 3.3 验收标准（v3(final) 已全部通过）
- G：拿到 .html → W：双击 → T：面板即用；无常驻进程；无 pip install / venv / 注册表写入。
- G：长内容 → T：**无页面滚动条**，画布满屏（70/16/16/16 实测），内容靠平移浏览。
- G：块内编辑 → T：textarea 原生编辑，回车/Backspace 无任何代码干预；换行/空行原样保存（实测 `\n\n` 保留）；高度随内容自动增长（212→279px 实测）。
- G：两块重叠 → T：任意层**拖动手柄仍可见可点**（真机：三块全叠把手可命中可拖动）。
- G：拖空白 → T：画布平移，刷新后视角保持。
- G：任一块 → W：真实点击「复制」→ T：该块文本进剪贴板（toast「已复制该块」实测）。
- G：点「复制全部」→ T：**按钮不存在**（已移除）。
- G：v1/v2 旧数据 → T：自动迁移为 v3 布局（内容与位置不丢）。
- G：导出 → 得 .json（`分镜提示词_<标题>_<时间>.json`）；换机导入 → 完整还原（含位置与视角）；可撤销。
- G：关闭浏览器 → T：无残留进程/文件（除用户自导出的 .json）。

## 4. 方案设计（SAD v3(final)）

### 4.1 架构
```
storyboard-prompt-panel.html（单文件，模板即源码，全内联，约 20KB）
  ├─ HTML：顶栏（fixed）+ 画布（fixed 铺满视口）+ 内层 board（transform 平移）
  ├─ CSS：把手 z-index 9999 置顶、overflow:hidden 无滚动、textarea autoResize
  └─ JS：原生（无第三方库）
        ├─ 状态 v3 + localStorage 防抖保存 + 双兜底 flush
        ├─ 渲染：textarea 块 + 顺序徽章（round(y/40) 行内 x）
        ├─ 交互：mousedown 分发——把手=块拖拽（transform 跟随→落定）、空白=画布平移、块内=编辑
        ├─ 单块复制：clipboard API → execCommand 兜底；克隆错位落点
        └─ 导入/导出：FileReader + Blob；迁移函数 migrate() 兼容 v1/v2/v3
运行：双击 → file:// 直开；无后端、无端口、无网络（全离线）
```

### 4.2 数据模型（v3）
```json
{
  "app": "storyboard-prompt-panel",
  "version": 3,
  "title": "未命名分镜",
  "pan": { "x": -220, "y": -140 },
  "blocks": [
    { "id": "b_1a2b3c", "text": "远景，雨夜小巷……", "x": 20, "y": 20 }
  ]
}
```
- v1 迁移：丢弃 `title` 字段，按网格 `(20+i%4*360, 20+⌊i/4⌋*150)` 补 x/y；v2 迁移：补 `pan {0,0}`。
- localStorage key：`storyboard-prompt-panel:v1`（沿用，内容 v3 格式）；导入备份 key 同后缀 `:backup`。
- 导出文件名：`分镜提示词_<标题或日期>.json`

### 4.3 UI 布局（极简主路径）
```
┌─────────────────────────────────────────┐
│ [分镜标题]  [+ 新增块] [⭳导出] [⤒导入]         │ ← 顶栏 fixed，4 元素
├─────────────────────────────────────────┤
│  ╔═ 无限画布（fixed 铺满视口，无滚动条）══════╗ │
│  ║ [⠿]#1 ┌──────────┐   [⠿]#2 ┌──────┐  ║ │ ← 块：把手(9999) + 原生 textarea + (#序号/操作)
│  ║      │ textarea…│        │ …    │  ║ │     操作 hover 浮现；重叠时把手浮顶
│  ║      └──────────┘        └──────┘  ║ │
│  ╚════════════════════════════════════╝ │
│  hint：无限画布/顺序/备份 提示（左下 fixed）    │
└─────────────────────────────────────────┘
```

### 4.4 关键实现点
- 无限画布：`html,body{overflow:hidden}`；`.canvas{position:fixed;top:70;16,16,16}`；`.board{transform:translate(pan)}`；块坐标不限；空白 mousedown → **先 blur 当前聚焦元素（取消输入焦点）** 再启动 pan（实时 transform，mouseup 落盘）。
- 把手置顶：`.drag-handle{z-index:9999; background:rgba(255,255,255,.92); border}`；`.block:hover{z-index:7000}`；`.block.drag{z-index:8000}`。
- 块编辑：`<textarea>` 原生；不做任何 keydown/输入干预；`input → b.text = value + autoResize + 防抖保存`。
- 块拖拽：`mousedown(handle)` 记录起点/原坐标 → `mousemove` transform 跟随（会话内记录 lastX/lastY）→ `mouseup` 换算新坐标（无 move 则原地）、更新徽章、保存。
- 新增块：落在**当前视口中心**（`-pan + 视口中心 - 块半宽` + 小错位）；克隆：原块 `+(28,28)`。
- 单块复制：`text.trim()` → clipboard API → execCommand 兜底。
- 顺序徽章：`sequenced()` 排序（行容差 40px），渲染/拖拽后重算。

## 5. 里程碑

| 里程碑 | 内容 | 验收出口 |
|---|---|---|
| **M1 骨架（P0）** ✅ | 垂直列表版（已被 v2 取代） | 已通过 |
| **v2 自由画布** ✅ | 画布化、无标题、全文展开、位置推导 + 徽章 | 已通过（14 项全绿） |
| **v3 无限画布** ✅ | 视口锁定无滚动 + 平移；把手置顶可重叠；数据 v3 + 迁移 | 已通过（全绿） |
| **v3(final) 调整** ✅ 2026-09-07 | 移除「复制全部」；回车分段改进回退为原生 textarea | **已通过**：回归全绿（无复制全部 / textarea 原生 / 换行空行原样 / autoResize / 真实鼠标拖拽+平移 / 单块复制真实手势 / 导出） |
| **M3 发布** | 真机四项验收复测 + Seedance 演示示例实测 + 归档 | 交付 .html + `03_发布说明.md` |

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| file:// 下 localStorage 被浏览器策略清理 | 「导出 JSON」硬备份；左下 hint 提示定期导出 |
| 剪贴板权限（Firefox file://） | execCommand 兜底 + 失败 toast |
| 块拖出视口找不到 | 拖空白平移找回；导出/导入含全部块坐标 |
| 顺序语义依赖位置 | 徽章仅供展示；用户自行摆位决定 |

## 7. 下一步

- ✅ v3(final) 已交付：`storyboard-prompt-panel.html`（19.6KB 单文件），headless Edge 真机回归全绿。
- 等用户双击试用收口 → **M3 发布**：真机四项验收复测 + Seedance 演示示例实测 + `03_发布说明.md` 归档。
- 验收测试数据取自 `已封装的资料\Seedance2-提示词资料\01_演示示例集.md`。