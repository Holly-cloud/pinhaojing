# 交接档案 · v7.17–v7.20 四轮浓缩（2026-09-18 ~ 09-20 · 协议建立前的首批存档）

> 目的：把协议建立之前、只存在于会话里的四轮「口径裁决 / 根因 / 教训」补录成档，供接手 agent 考古。
> 每轮完整细节见 `dev/CHANGELOG.md` 对应版本节与 `_qa/snapshots/BASELINE_v<版本>.md`。

## v7.17 逗号转空格（台词除外）｜ 2026-09-18

- **口径（Holly 逐条拍板）**：仅中文逗号 `，` → **半角空格**；中文双引号成对包住的台词区不动；**只做「放大编辑」弹窗**（写作台右栏明确不放）；toast 带撤销；无变化提示。
- **关键裁决**：台词判定**必须复用 v7.7 的 `hlDialogueMask`**（不重写，避免与着色层口径分裂）；QA 提议把"撤销不可点"断言降级为程序化 `.click()` 换闸门变绿 → **主理人拒绝**（弱化断言），改修产品。
- **真缺陷**：`.toast` z-index 99 < 弹窗 11000 → 弹窗内所有提示（含撤销）被遮、真机点不到（QA 用 `elementFromPoint` 取证）→ z-index 抬到 13000。
- **教训**：快速模式的功能也必须**当场补常驻断言**（工程师临时探针用后即删 = 零保护）；E 组 16 条全部定向变异证伪。

## v7.18 多项目容器 + 两个修复 ｜ 2026-09-19

- **口径**：多项目 = 各自独立可切换（非清空重置）；新建 = 零块；删除有确认+撤销、唯一项目禁删；`collapsed` 按项目隔离；`templates`/`cmpl` 全局共享。
- **架构裁决**：`state.blocks` 保持"当前项目的块"语义，切项目**替换引用**（镜像）→ 消费方零改动；兜底 `syncActiveProject()` 挂 `saveNow()` 与 `switchProject()`（防 `filter()` 重赋值解链——QA 专门构造解链场景验证）。
- **命名缺陷**：新建项目默认名 `项目 N` 首版用 `length+1` → 跳号 + 删后重名 → 改扫现存名取 `max(N)+1`。
- **根因（间距不均）**：`setView('canvas')` 先 render 后 applyView → 隐藏态测高得 0 → 全块塌 126px；修 A（次序对调）+ B（隐藏态不写 0）双保险。**架构师逐一排除了主理人最初的 migrate 坐标兜底假设**（猜错了）。
- **迁移观感验证**：同一份 v16 数据注入 v7.17/v7.18 两版本 → 真读 rect **Δmax=0px**。
- **工装地雷**：`test-artifact.mjs` 用 `String.replace` 拼产物 → 源码含 `` $` `` 即把 `<head>` 复制一份、脚本非法（套件报 `state is not defined` 不指向根因）→ 已改 `indexOf`+`slice`，注入证伪三序列全过。

## v7.19 划选重影 + 补全写回 + 滚动重定位 ｜ 2026-09-20

- **重影根因**：`.blk-input::selection` 把选中文字强制显形 → 与底层彩色层**精确叠印**（两层几何偏移实测为 0——**"错位重影"假设被推翻**）。修法只留半透明蓝底；`select` 事件改 `hlStatus`（状态栏与着色层同源）。QA 用**像素直方图**证明：现写法主墨色 = 族色∘28%蓝底理论值；还原旧写法则 `#1d1d1f` 221px + 族色塌 0。
- **方向修正（Holly 澄清）**：她要的"浮动气泡"**不是** # 触发的候选列表 → 催生 v7.20 气泡群；# 补全定位为"精确查找"并存。
- **封存→重裁**：长文本文末输 # 气泡刚弹即关（`scroll→cmplClose` 既有行为）→ Holly 先裁决"封存 # 功能" → 工程师发现封存使 `verify_v78` 14 条真机断言**硬崩** → **重裁为修根因**（`scroll→cmplPlace` 重定位，1 行）。教训：**裁决前要把代价摆全**。
- **交付纪律**：根 README 陈旧问题第三次出现 → 此后每轮任务书钉死"必须同步根 README"。

## v7.20 项目名常显 + 灵感气泡群 ｜ 2026-09-20

- **需求溯源**：Holly 报「项目按钮位置不合适」→ 审计发现本质是**项目身份全 UI 不可见**；报「写作中未发现气泡」→ 根因是 # 触发零可发现性 + 画布内联未绑定 → 引出气泡群正解。
- **方案取舍**：五方案中她选方案一（项目名常显）；方案二（复制骨架）/方案三（单项目导出）未采纳，留候选。
- **质量链**：QA 20/20 独立探针 + G6 隔离四步复核（改坏验证证明隔离受监督）；工程师抓到 `.wdb-cluster` visibility 短路继承的真 CSS 缺陷。
- **断言升格**：Holly 核准 17-18 条 → QA 抓出探针**夹具恒真**（fixture 首行含目标插入文本）→ 工程师两度被会话中断未开工 → **主理人亲自接线**（修夹具 + 18 条 + 闸门级证伪 17/18❌→还原 15/15）。
- **教训**：会话中断后**磁盘是唯一可信进度**；agent 两度没接上活就直接自己接手是合理选择。

## 未决项（截至本档案）

见 `HANDOVER.md`「待用户裁决项」。
