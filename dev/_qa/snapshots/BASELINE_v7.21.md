# BASELINE · v7.21（写作台块标签「初/补」+ 大纲分组排列 + 项目总览面板）

> 交付日：2026-09-21 ｜ 前序基线：`BASELINE_v7.20.md`
> 本文件是**当前基线**；`dev/CHANGELOG.md` 是历史，本文件是现状。
> ★ 本版本含**两轮交付**：**v7.21**（首版，两项需求）与 **v7.21b**（同日 UI 重构：项目总览面板低门槛改版）。快照 `PHJ_v7.21_2026-09-21.html` 按「同版本内迭代」口径**同名覆盖**。

## 一、指纹与验收

| 项 | 值 |
|---|---|
| 版本 | **v7.21 / v7.21b** |
| `state.version` | **17**（★**未升版**：`block.tag` 为纯增量可选字段，迁移零丢失；升版无收益且会扰动四个套件的 `version === 17` 断言） |
| `LS_KEY` | `storyboard-prompt-panel:v1`（**不变**，P3） |
| bytes | **347218**（v7.20 324783 → v7.21 338465 → **v7.21b 347218**） |
| sha256 | `5426d5e7e7a8cbc1f60643dd003809691a88f33207d3ebffd6340e1b4364e986` |
| 快照 | `PHJ_v7.21_2026-09-21.html`（= 产物，`cmp` 逐字节一致；等价性闸门默认基准） |
| 闸门 | `node dev/_qa/run-gate.mjs` = **16/16 全绿**（约 107s） |
| skin-guard | R3-A = **15**（未顶穿）／ R3-B = 0 ／ R3-C 非真空 ／ R4 可摘除 |
| `PHJ` 对外面 | **136 名不变**（18 模块键；v7.21b 经 QA 与 `git show HEAD` 逐字 diff 确认） |

闸门逐项：`347218 B` ｜ 等价性 PASS ｜ 83/83 ｜ F 18/18 ｜ G 16/16 ｜ v7.8 **54/54** ｜ 开发态 18/18 ｜ W 21/21 ｜ C 31/31 ｜ E 16/16 ｜ P 4/4 ｜ M 9/9 ｜ V 4/4 ｜ v7.19 10/10 ｜ v7.20 18/18 ｜ **v7.21 11/11**

## 二、变更

### 第一轮（v7.21）· 需求一：写作台块标签「初/补」+ 分组排列

| 文件 | 改什么 |
|---|---|
| `core/persist.js` | `migrateBlocks()` **两处投影**纳入 `tag`（脏值/缺失归 `''`，零丢失）—— **不改版本** |
| `core/store.js` | `defaultState()` 示例块 `tag: '初'` |
| `view/write.js` | `wdTagOf` / `wdGroupHead` / `wdCycleTag` 新增；`renderWrite()` 分组渲染；`wdRow()` 加标签钮；`wdReorder()` 加跨组守卫；`wdNew()` 默认 `tag:'初'`；DCL 内 `#wdList` 委托加 `.wd-lab` 分支 |
| `view/modals.js` · `interact/paste.js` | 另两处建块点默认 `tag:'初'` |
| `styles/55-write.css` | `.wd-group` / `.wd-lab` 样式 + 加入 reduced-motion 直切清单 |

**设计裁决**：分组走**呈现层**（`order` 语义不变 → 拼接栏/导出零改动）；组头独立 class `.wd-group`（**绝不能是 `.wd-item`**，否则打断 W4）；行内 idx 一律全局序位；仅 ≥2 种标签才出组头；跨组拖动被拒。

### 第一轮 · 需求二：移除顶栏项目按钮 —— **已取消**（Holly 指令），**零改动**

### 第一轮 · 需求三：产品内「项目总览」面板（首版）

`view/modals.js`（`MAP_*` + 渲染）· `index.html`（`#btnMap`「览」+ `#mapMask`）· `shell/wiring.js`（元素级接线）· `interact/keys.js`（`closeTopLayer()` 加一层分支）· `styles/40-window.css`。
口径：完善度 = 闸门覆盖 × 上线状态；防漂移 = 数据手写 + `verify_v721` B3 交叉校验。

### 第二轮（v7.21b）· 项目总览面板低门槛改版 ★

> 触发：Holly 实测首版面判定「**可读性极差**，我们需要**低门槛阅读**」。真机截图量化 5 项病灶：`320px` 窄窗 · 字号 12px/11px · 第二行是**文件路径 + 套件代号**术语墙 · 10 个「完善」徽章**同色零信号** · 末条被裁需滚动。
> 根因：**把「给开发者的审计证据」当成了「给用户的阅读内容」**。Holly 裁决：①加宽并**两栏** ②技术信息**折起来** ③完善度改**三档人话状态 + 彩色点** ④**按用途分组**。

| 文件 | 改什么 |
|---|---|
| `view/modals.js` | `MAP_FEATURES` 加 `group`/`desc`、`state` 值域改 `done`/`pending`/`plan`；新增 `MAP_STATE_TEXT` / `mapStateText` / `mapDot` / `mapBuildTop` / `mapBuildList` / `mapTechRow` / `mapDetailNode` / `mapSelect` / `mapBuildNote`；重写 `mapBuildDom` / `mapFeatureRow` / `openMapPanel` |
| `index.html` | 内层窗加宽度**变体类** `.modal.map-win`（★不动基础 `.modal`） |
| `styles/40-window.css` | `.map-win`（`min(880px,92vw)` × `min(78vh,680px)`）· `.map-cols`（236px / 1fr 两栏）· `.map-list`/`.map-group`/`.map-row.on`/`.map-dot`/`.map-detail`/`.map-dname`/`.map-tech`/`.map-stamp` 等；字号上调（全面板 **≥12px**）；三档点色（done 绿 `#34a853` / pending 琥珀 `#d97706` / plan 灰）；`@media (prefers-reduced-motion:reduce)` 内 `.map-row{transition:none}` |

**形态**：左栏 `.map-list`（写作 5 / 画布 2 / 项目与界面 3，白话名 + 彩色状态点）+ 右栏 `.map-detail`（功能名 16px / 说明 / 状态徽章 / 版本徽章 / **技术证据两行**：代码位置 + 验收证据）。**右栏即天然的「技术细节位」** ⇒ 无需再叠折叠控件。点击左栏项 → 右栏联动（元素级 click 委托，未新增全局监听，守 E7）。默认选中第一项。

**★不撒谎（收口前修补）**：健康行原为**写死静态文案**「16 道自动验收全部通过」——将来闸门变红时面板仍会宣称全通过（`file://` 离线单文件**无法运行时探测闸门**）。改为**动态取数**（`MAP_FEATURES.length` / `MAP_GATE_TOTAL`）+ 句末弱色时效标记 `.map-stamp`「**（交付时）**」，注记追加「本面板为交付时状态；验收的实时结果以 `node dev/_qa/run-gate.mjs` 为准」。

**可读性实测（主理人真机 CDP 取证）**：面板 **880×680** ｜ 名称截断 **0/10**（首版 2/10）｜ **无需滚动**（首版必须滚）｜ 窄视口 1600/1280/1024/956/900/820px **六档均零截断、零溢出**（首版 320px 固定宽度下长行折行）。

## 三、工装变更

- **第一轮**：新增第 16 道闸门 `verify_v721.mjs`（**11 条** = A×6 + B×5）；`run-gate.mjs` **T10 四处** + 退出码 **19**；`verify_build_equivalence.mjs` 的 `OLD` → v7.21 快照；`INDEX.md` 当前基准提升
- **第二轮**：`verify_v721.mjs` **仅 B2 判定式 + B 组页内助手适配**（见 §四）——**B1/B3/B4/B5 判定式逐字未动、条数仍 11**；`run-gate.mjs` 的 `[1]` 期望值随产物同步两轮

## 四、★证伪记录（T7 / T8）

| 轮次 | 破坏内容 | 结果 |
|---|---|---|
| **R1**（首版） | 从 `migrateBlocks` 末尾投影**删掉 `tag`**（即「静默剥离」那个致命点） | **退出码 19**、`[16] → 9/11`；**A1 + A2 红**（A2 明细 `reload后={"has":false}` 正是剥离现场） |
| **R2**（首版） | 同时 7 处：组头误用 `.wd-item` · `wdCycleTag` 不写 state · 删跨组守卫 · 删 `keys.js` 的 mapMask 分支 · `MAP_ASSERT_TOTAL` 改错 · 完善度徽标不渲染 · reduce 清单删 `.wd-lab` | **退出码 19**、`[16] → 2/11`；**A3/A4/A5/A6/B1/B2/B3/B4/B5 全红** |
| **R3**（v7.21b） | 破坏「点击左栏 → 右栏联动」（点击不再调 `mapSelect`）；先刷等价基线以触达 `[16]` | **退出码 19**、`[16] → 10/11`、**仅 B2 ❌**、干净报红；B2 明细显示点后右栏未切换、`on` 仍 0 |

⇒ 首版 11 条断言全部取得证伪证据（R1 覆盖 A1/A2，R2 覆盖其余 9）。第二轮 R3 覆盖 B2 的新增联动检查。
破坏后**还原并复核指纹**：`sha256` 精确回到基线（`2b2551d5…` / `5426d5e7…`），构建确定性两次得证。

**★R2 顺带抓出套件自身缺陷**：破坏把组头变成 `.wd-item` 后，助手 `__rowInfo` 对无 `.wd-no` 的行直接 `null.textContent` → **套件崩溃（exit 1）**，run-gate 报「未取到汇总」而非逐条红。→ 断言应当**报红而不是崩掉**，已加**空值防护**。**这条缺陷是被证伪环节发现的，不是被测试通过的。**

**★主理人复核抓出 B2 一处「净减」（v7.21b）**：工程师首版适配时顺手删掉了 `st.todo === md.todoN`（该检查在两栏结构下**仍完全可做**——`__mapState().todo` 与 `__mapData().todoN` 两个助手都还在算），属 **T11「不得为凑绿弱化判定」** 违规 → 打回补回，并把硬编码 `st.heads === 3` 一并改为**数据驱动** `st.heads === md.groups`。**教训：结构变了只应改编排，不应改判定强度。**

## 五、已知遗留（不阻塞）

1. **`verify_v78`（[6]）低频偶发红**：既有记录 **1/3** + 接手轮实跑 **0/4** + v7.21b 轮实跑 **0/10**（10 份日志 [6] 全为 `H+I 54/54`）→ **累计 1/17 ≈ 6%**，按 **T9 留观，未改任何断言**。★下次复现**先保存 run-gate 完整原始输出**再定位。
2. **★`B2` 的 `st.todo === md.todoN` 当前恒真（`0 === 0`）**：10 项功能全为 `done`，该子句**今日无防护信息量**。QA 已实测证明其**齿在抓「数据 ↔ DOM 分叉」**（只改数据不改渲染**不会**红；渲染不回写 `data-state` 时才红），**不是**「改数据即红」。→ 待出现真实 `pending` 项时自然获得齿感；如要立即恢复，需补一条**含 pending 的定向夹具断言**（属断言升格，须先报 Holly 核准，本次未做、未擅自扩范围）。
3. **健康行的「（交付时）」为设计上的诚实妥协**：面板是 `file://` 离线单文件，**无法运行时探测闸门状态**，故时效只能显式标注、不能自动校验。B3 交叉校验覆盖「道数 / 条数一致性」，**不覆盖「是否真全绿」**。
4. Linux 侧跨平台首巡回（在途工作，Windows 侧已完成）。
5. `pointer.js` 130ms 定时器理论时序窗口（v7.18 QA 记录，未能复现，留观）。
6. 「同批刷新 `HANDOVER`」已由**交付惯例**固化；`IRON_RULES.md` 的 **C2 已改为道数无关**，并新增 **C7**（推送时机由 Holly 自行安排）。

## 六、结论

- **v7.21 两项需求功能完整**；**v7.21b** 面板低门槛改版**实测达成四项口径**（880 两栏 / 白话优先 / 按用途分组 / 三档人话状态，全面板 ≥12px、0 截断、免滚动）。
- 两轮均**对既有 15 道闸门零回归**（每轮全绿）；新功能**有常驻断言守门且逐条证伪**（T5 零覆盖盲区已消除）。
- v7.21b **未新增源文件** ⇒ 未触碰 `E5`（SLICES 20 条）/`E8`（136 名）；`skin-guard` 未顶穿（R3-A 仍 15）；未新增全局按键监听（E7）。
- **可交接**：`git status` 干净 + 闸门 16/16 + `HANDOVER` 与实际一致。
