# 拼好镜 · 版本沿革（倒序）

> 本文件是**版本沿革（倒序）**——从最新版本往回排（v7.17 → v6.1）。
> **v6 系列的详细发布说明与验收记录**见 `dev/docs/releases/`。
> **当前现状**以 `dev/_qa/snapshots/BASELINE_v7.19.md` 为准（本文件是历史，不是现状）。
> 来源说明：以下逐版小节自仓库根 `README.md` **原样搬入**（2026-09-17 结构整理），**正文一字未改**，仅位置搬移。
> 补记（2026-09-18）：v7.15 一轮**漏记**本表，本次随 v7.16 一并**补记**（见下两节）。

## v7.19 · 划选重影修复 + 写作台补全写回/触发修复（缺陷修复 · A' 撤销封存改修根因）

> 需求① 划选文字「重影/双重叠」与需求② 写作台右栏补全（上屏未实时写回 + 触发根因）；逐项证据与基线见 `dev/_qa/snapshots/BASELINE_v7.19.md`。

| 项 | 内容 |
|---|---|
| **划选重影** | 根因 = `styles/50-editor.css` 的 `::selection` 把 `-webkit-text-fill-color`/`color` 覆盖为不透明深色 → 选中时上层编辑层的字显形，压在底层彩色层同一份字；修复 = **只保留半透明蓝底，去掉文字显形覆盖**；实测选中时编辑层墨迹由 6184 px → 0 px，蓝底仍在 |
| **划选抖动** | `select` 事件由整层 `hlRefresh` 改为仅 `hlStatus`（状态栏：行/列/节/错误数），实测键盘 Shift+→ 12 次触发整层重建次数 25 → 12 |
| **写作台补全写回** | 根因 = `complete.cmplCommit` 上屏后只改 `textarea.value`、没触发写作台「输入即写回」路径 → `state.blocks` 仍是旧值；修复 = 在 `setSelectionRange` 后补派一次 `input` 事件；实测上屏后 `block.text === ta.value`，切条往返不丢字 |
| **A' 触发根因修复** | 曾短暂以 `CMPL_TRIGGER_OFF` 封存 `#` 触发（根因 = 长文本文末输入 `#` → textarea 自动滚动 → 旧 `scroll → cmplClose` 监听刚弹即关）；**Holly 重裁方案②：撤销封存**（开关与两处短路守卫全删），scroll 监听改为 **`cmplPlace(host)`**（滚动只重定位不关闭）→ 自动滚动不再误杀气泡、用户滚轮气泡跟随；真实浏览器三场景验证通过 |
| **触发条件** | 补全由 `#` 触发，**两宿主均可用**（撤销封存后恢复）；弹/位置/裁剪/键盘/鼠标/滚动跟随均与弹窗一致 |
| **数据契约** | `state.version` 仍 **17**，持久结构无变化；`skin/corpus.js` 一字未改 |
| **对外面 +1 名** | `PHJ.highlight` + `hlStatus`（`write.js` / `wiring.js` 跨模块引用） → `P2-B2` 由 135 → **136** 名（`P2_MODULES` 仍 18） |
| **纪律** | 不新增 `keydown/keyup/blur` 监听；皮肤层未改；新中文文案未命中皮肤词元 |
| **产物** | 304151 → **306977 B**（+2826）；sha256 `dfca622f62d0124ef6ee9dc21dac783cb27ce707c7b5f6928dfb9e6f8602cf52` |
| **闸门** | `node dev/_qa/run-gate.mjs` = **14/14**（83/18/16/**54**/18/21/31/16/4/9/4 + **v7.19 组 10/10**）；既有 13 道断言条数不变，仅期望值/快照/导出面同步；新增第 14 道 `verify_v719.mjs`（重影 G×7 逐像素 + 补全可用 K×3；配套新共享库 `lib/png.mjs`）；门级证伪（回退 scroll 修复 → [14] 红 / 退出码 17 → 还原 → 14/14）；`skin-guard` 仍 **R3-A=15 / R3-B=0** |
| **基线** | 新 `dev/_qa/snapshots/BASELINE_v7.19.md`；新快照 `PHJ_v7.19_2026-09-20.html`；等价性 `OLD` → `PHJ_v7.19_2026-09-20.html` |

## v7.18 · 多项目容器 + 需求②「间距不均」渲染时机修复（结构增量 + 缺陷修复）

> 需求③（多项目）设计 `dev/docs/design/多项目_设计_2026-09-19.md`；需求②根因调查 `排布错位_调查`；
> 逐项证据与基线见 `dev/_qa/snapshots/BASELINE_v7.18.md`。

| 项 | 内容 |
|---|---|
| **多项目容器** | `state.projects[]` + `activeProject`；顶层 live 字段（`blocks/pan/zoom/splice/title/collapsed`）为**活动槽同引用镜像** → 既有消费者（canvas/write/splice/pointer/paste/keys/overlay）**零改动**；新增 `newProjectSlot/projectAt/syncActiveProject/loadProjectInto`（`core/store.js`） |
| **项目 UI** | 顶栏 `#btnProj`（固定文案「项目」）复用 `#ctxMenu`：逐项 `●/○ 名` + 新建/重命名/删除（`view/modals.js`）；`openModal` +可选 `message`；删除唯一项目被拒、删活动项先切邻居、删除可**撤销** |
| **迁移 v16→v17** | `migrate` 拆 `migrateTemplates/Cmpl/Blocks/Splice/OneProject`；老数据**恰合成 1 个项目**、`blocks/pan/zoom/splice/title/collapsed` **逐字保留**（单项目路径字节等价）；`load()` 阈值 `<17`；`sanitizeState` 逐项目剔图；`saveNow` 先 `syncActiveProject`（`core/persist.js`） |
| **需求② 渲染时机** | `setView` **先 `applyView()` 再 render**（方案A：画布在**可见态**被测量）+ `autoResize` 隐藏态 `scrollHeight===0` **不写 `height:0px`**（方案B 加固）（`view/write.js` / `view/canvas.js`） |
| **paste 修复（并入）** | `interact/paste.js`：由「列举 3 个容器 class」改为**判断目标是否可编辑**（`TEXTAREA`/`INPUT`/`isContentEditable`）∪ 保留旧三容器 → 编辑器内 Ctrl+V 不再被画布抢占 |
| **新建项目默认名** | `nextProjectName()`：现存名里 `^项目 (\d+)$` 的最大号 **+1**（无则从 1）→ 不跳号、**删项目后不重名** |
| **数据契约** | `state.version` **16 → 17**；`LS_KEY` 不变；`templates`/`cmpl` **全局共享**、`splice` **逐项目** |
| **对外面 +9 名** | `PHJ.store` +4（`loadProjectInto/newProjectSlot/projectAt/syncActiveProject`）、`PHJ.modals` +5（`deleteProject/newProject/openProjectMenu/renameActiveProject/switchProject`）→ `P2-B2` 126 → **135** 名（`P2_MODULES` 仍 18） |
| **纪律** | `skin/corpus.js` **一字未改**（仍 `SLICES` 第 9 位）；**不新增任何 `keydown/keyup/blur` 监听**（`#btnProj` 仅 1 个 `click`）→ `P2-A`/`P3-A` 逐字不变；不改 `LS_KEY`、无外部依赖 |
| **产物** | 288444 → **304151 B**（+15707）；sha256 `df22b5903fdcbe39cc6eae873b40689b6ed9fc37a7de46eb2609f2f848f91e71` |
| **闸门** | `node dev/_qa/run-gate.mjs` = **13/13**（83/18/16/**54**/18/**21**/**31**/**16**/**4**/**9**/**4**）；断言**条数不变**，仅期望值/快照同步；`skin-guard` 仍 **R3-A=15 / R3-B=0** |
| **基线** | 新 `dev/_qa/snapshots/BASELINE_v7.18.md`；新快照 `PHJ_v7.18_2026-09-19.html`；等价性 `OLD` → `PHJ_v7.18_2026-09-19.html` |

## v7.17 · 放大编辑「逗号转空格（台词除外）」（编辑器文本工具）

> 增量需求（口径由主理人与 Holly 逐条锁定）；逐项证伪见 `dev/_qa/snapshots/BASELINE_v7.17.md`。
> 一句话：在「放大编辑」弹窗（`#blkMask`）底部动作区新增一个按钮，把**编辑器内全文**的**中文逗号 `，`**换成**半角空格**，
> **中文双引号“ ”成对包住的台词区内的逗号保持不动**。

| 项 | 内容 |
|---|---|
| **入口** | `#blkMask` 的 `.modal-actions` 内新增 `<button class="btn" id="blkComma" title="…">逗号转空格</button>`，位于 `#blkStrip`（移除空行）与 `#blkCopy`（复制全文）之间（`src/index.html`） |
| **处理对象** | **仅中文逗号 `，`（U+FF0C）** → **半角空格 ` `（U+0020）**。**半角逗号 `,`（U+002C）与顿号 `、` 不处理** |
| **台词区豁免** | ★**直接复用 v7.7 已有的 `hlDialogueMask(text)`**（`editor/highlight.js`）：**一行内成对中文双引号“ ”包住范围**内的逗号一律不动；**换行重置 / 未闭合不生效 / 半角 `"` 不算**（随既有口径）。**不另写判定** → 与着色层同源、不脱钩 |
| **作用范围** | 编辑器内的**全部文本**（非选区） |
| **只改一个编辑器** | ★只改「放大编辑」弹窗；**写作台右栏（`#writeDesk`）不动**（Holly 明确只在此弹窗） |
| **核心函数** | 新增**纯函数** `hlCommaToSpace(text)`（`editor/highlight.js`，返回 `{ text, n }`，`n` = 实际替换处数）——句内复用 `hlDialogueMask`（**不导出**，仅模块内） |
| **接线** | `shell/wiring.js` 既有 `DOMContentLoaded` 块内给 `#blkComma` 加 **`click`**：`hlCommaToSpace(ta.value)` → `ta.value = r.text` → `fitBlkWidth()` → `hlRefresh()` → `cmplReset()`（清候选/槽位临时态）→ toast |
| **取消语义** | 弹窗为「确定/取消」语义：**只改 `textarea`**，**不直接改 `state.blocks`**（避免破坏取消；点「确定」才写回块） |
| **撤销** | 闭包捕获旧 `value`，`toast('已转换 N 处逗号', {label:'撤销', fn: restore})`；撤销恢复 `ta.value` 并重渲染。**`n === 0` 时** → `toast('没有可转换的逗号')`，**不给撤销** |
| **对外面 +1 名** | `PHJ.highlight` `+ hlCommaToSpace`（`wiring.js` 跨模块引）→ `P2-B2` 由 125 → **126** 名（`P2_MODULES` 仍 18 不变） |
| **纪律** | `skin/corpus.js` **一字未改**（仍 `SLICES` 第 9 位）；**不新增任何 `keydown/keyup/blur` 监听**（只加 1 个 `click`）→ `P2-A`/`P3-A` 逐字不变；**`state.version` 保持 16**（不改持久结构，不升版本）；不改 `state.blocks`、不改 `LS_KEY`、无外部依赖 |
| **产物** | 285848 → **288444 B**（+2596）；sha256 `d943d616672f5a2ef79528b61f175804fa6b75a5fb66c9a26601015d902fd4b8`（首个构建 288249 B → 就地补丁 +195 B，见文末补记） |
| **闸门** | `node dev/_qa/run-gate.mjs` = **10/10**（83/18/16/**54**/18/**21**/**31**/**16**；E 组为 QA 另立的**第 10 道**）；既有 9 道判定式与条数零改动；`skin-guard` 仍 **R3-A=15 / R3-B=0** |
| **基线** | 新 `dev/_qa/snapshots/BASELINE_v7.17.md`；新快照 `PHJ_v7.17_20260918.html`；等价性 `OLD` → `PHJ_v7.17_20260918.html` |

> **补记（2026-09-18 · **并入 v7.17，就地更新，不新开 v7.17.1**）**：
> QA 复核发现一处**真缺陷**——本按钮的 toast 反馈（含「撤销」）在「放大编辑」弹窗打开时被弹窗遮挡
> （`.toast` `z-index:99` < `.modal-mask` `z-index:11000`；弹窗高约 94vh，底心 toast 落于其内），视口高偏小时**真实鼠标点不到「撤销」**。
> **修法（就地补丁，只此 1 处 CSS）**：`styles/10-canvas.css` 的 `.toast` `z-index: 99 → 13000`（> `modal-mask` 11000、> `cmpl-order-pop` 12050，**全域置顶**）——不避让、不重定位。
> 同时 QA 为该工具另立**常驻断言组** `verify_e.mjs`（**E 组 = 16 条**，run-gate 挂为**第 10 道闸门**，退出码 13）。
> 产物 **288249 → 288444 B**（+195，仅 1 行 CSS 注释 + z-index 位数），sha256 更新为 `d943d616…fd4b8`；
> `state.version` 仍 **16**、`skin/corpus.js` **一字未改**、`skin-guard` 仍 **R3-A=15 / R3-B=0**。

## v7.16 · 写作补全四项增强（A/B/D/H）（候选引擎「越用越顺」）

> 需求定义 `写作补全_初衷复盘与改进建议_2026-09-18.md` §3；架构设计 `写作补全四项增强_设计_2026-09-18.md`；
> 逐项证伪与口径见 `dev/_qa/snapshots/BASELINE_v7.16.md`。

| 项 | 内容 |
|---|---|
| **A · 状态栏假错误** | 把「行级结构区」落到「**字符级豁免掩码**」：新增 `struct.structExemptMask(text)`（**领域策略**，只活 `struct.js`），`highlight.hlClassify(text, exempt)` 增**可选**掩码参、R0 错误分支加 `&& !(exempt && exempt[i])`。**风格区（`风格：`）/硬性要求区（`硬性要求：`）的逗号**不再计入错误数、不标红、随层级族色；**正文/起手式（`anchor`/`body`）照旧计数**（核心对照组）。着色层保持**零领域语义**（只吃不透明掩码），与 v7.7 台词区豁免**同层同构** |
| **B ·「未用过」变活的** | 候选渲染时**动态判定**条目是否已在用户文本出现：`cmplNeedle`（去 `${n}` → 最长连续 CJK 串）+ `cmplUsedText`（全部非图片块 ∪ 当前编辑器文本）+ `cmplIsUsed`（`needle.length≥2 && indexOf≥0`）+ `cmplNoteFor(it)`（唯一取值入口）。仅 `note==='未用过'`（实测 13 处）走动态；其它 note **永远静态**。`note` 字段**永不改写**；缓存经 `cmplUseInvalidate()` 在 **10 处**枚举失效 |
| **D ·「最近使用」加权** | 只在 `cmplCommit` **真正上屏**时记一次（`cmplUseTouch`）；**内容稳定 hkey** = `'h'+FNV1a32hex(group\0label\0body)`（免疫重排/换皮肤/未物化）；`useW = ln(1+n)·0.5^(ageDays/14)`（半衰期 14 天）作 `score` 相等时的**次级排序键**，**仅查询视图**（`cmplBuild`）；`cmplBuildGroupItems`/`cmplBuildGroups` **不改**（守 `H7` 语义） |
| **H · 语料体检（MVP）** | 配置窗 foot 新增 `#cmplCfgCheck`；**同窗视图切换**（`cmplCfgView ∈ {'lib','check'}`，不新开窗）：**重复句**（跨块、`len≥8`、`count≥2`、不在片段库）+ **风格包漂移**（vs 定型件 `cmplStyleRef()`）；唯一动作 = 重复句「**收进片段库**」。定型件经 `complete.cmplStyleRef()` **单一接缝**访问（不新增 skin 读者） |
| **数据契约** | `state.cmpl` 新增 `use: {}`；`state.version` **15 → 16**；`persist.migrate` 重建 `cmpl` 时**逐键保留 `use`**（跳 `__proto__`、只留 `n>0`、`t` 非数字回落 0）→ **零丢失**；`load()` 阈值 `< 16` |
| **对外面 +3 名** | `PHJ.struct` `+ structExemptMask`；`PHJ.complete` `+ cmplStyleRef` `+ cmplUseInvalidate` → `P2-B2` 由 122 → **125** 名（`P2_MODULES` 仍 18 不变） |
| **纪律** | `skin/corpus.js` **一字未改**（仍 `SLICES` 第 9 位）；**不新增任何 `keydown/keyup/blur` 监听**（H 只加 `click`；B/D 失效只在既有监听体内加行）→ `P2-A`/`P3-A` 逐字不变；`LS_KEY` 不变；无外部依赖 |
| **产物** | 264162 → **285848 B**（+21686）；sha256 `4e4803c7649240ff9926ace7a0b012e0c8ac7c3a244b79f0bc4b4099bc7d40c5`（含本轮把源码注释里误标的旧版本号统一为 v7.16，18 处、每处 −1 B） |
| **闸门** | `node dev/_qa/run-gate.mjs` = **8/8**（83/18/16/**54**/18/**21**）；**条数全部不变**（A/B/D/H 行为断言由 QA 另行落地）；`skin-guard` 仍 **R3-A=15 / R3-B=0**；顺带修正 `run-gate` 中间步骤行标 5 处 `[N/7]`→`[N/8]` |
| **基线** | 新 `dev/_qa/snapshots/BASELINE_v7.16.md`；新快照 `PHJ_v7.16_20260918.html`；等价性 `OLD` → `PHJ_v7.16_20260918.html` |

## v7.15 · 界面切换 + 写作台（胶囊分段控件 + 写作台视图 + 宿主解析层）

> 增量需求 `增量需求_界面切换与写作台_2026-09-18.md`；增量设计 `界面切换与写作台_设计_2026-09-18.md`；证伪见 `dev/_qa/snapshots/BASELINE_v7.15.md`。

| 项 | 内容 |
|---|---|
| **界面切换** | 顶栏新增**胶囊分段控件「写作｜画布」**，把「当前视图」提升为**会话态**（默认 **写作**，`state.activeView`）；画布视图原样保留 |
| **写作台视图** | 新增 `view/write.js` + `styles/55-write.css`：左大纲（拖手/序号/摘要/字数/高亮）+ 右大编辑器，两视图**同源**（同一份 `state.blocks`）；快捷键 `Alt+Enter/Backspace/↑↓/Shift+↑↓`、拖拽排序、`#wdStrip` 去空行、`#wdCopy` 复制全文、实时写回 |
| **宿主解析层** | 新增 `editor/host.js`（`makeHost`）：编辑器内核**宿主化**，`hostPopup`（`#blkMask`）/`hostDesk`（`#writeDesk`）复用同一 `highlight.js`/`complete.js` 内核 |
| **线性顺序** | 新增字段 `block.order`（`state.version` **14 → 15**，`migrate` 零丢失补齐；`normalizeOrder` 惰性归一 `0..N-1`） |
| **输入路由门控** | `keys.js`/`pointer.js`/`wiring.js` 加「写作视图分支」（**不新增任何 key 监听**）：方向键/空格/Ctrl+滚轮/双击空白在写作台内不动画布 |
| **产物** | 230515 → **264162 B**；sha256 `cc383db0d07c9f7e91f49093c2e1e1b6e40b9787dcb4c4cc4e55908d7e804087` |
| **闸门** | 新增 **W 组** `verify_w.mjs`（21 条）；`run-gate` **7 → 8** 道；`verify_v78` 仍 54/54 |
| **基线** | 新 `dev/_qa/snapshots/BASELINE_v7.15.md`；新快照 `PHJ_v7.15_20260918.html`；等价性 `OLD` → `PHJ_v7.15_20260918.html` |

## v7.14 · R2/R3/R4 + F1/F2/F3 收尾（架构收口，无产品行为变化）

> 本轮**不改产品行为**，只做架构收口；产物字节变化来自两处：R2 的 1 个导出键改名（`boot`→`wiring`，+2 B）与 F2 的 5 处陈旧指针（+21 B）。
> 逐项证伪与口径见 `dev/docs/decisions/R2-R4_模块改名与皮肤边界机制化_2026-09-17.md` 与 `dev/_qa/snapshots/BASELINE_v7.14.md`。

| 项 | 内容 |
|---|---|
| **R2 模块改名** | `dev/src/shell/boot.js` → `shell/wiring.js`（`git mv`，**只改名不拆**）；导出键 `PHJ.boot` → `PHJ.wiring`；`manifest` 的 `SLICES`/`MODULES` 与 `verify_v78` 的 `P2_MODULES`/`P2_EXPORTS_GOLDEN` 三处同步 |
| **R3 棘轮 + 零泄漏 + 防真空** | 新模块 `dev/_qa/lib/skin-guard.mjs`：**R3-A 棘轮**（皮肤词元命中引擎代码去重数 ≤ 冻结尾数 **15**，只堵新增）+ **R3-B 零泄漏**（皮肤长语料 **∪ 全部 `【…】` 完整标记串** 在非皮肤原文零命中）+ **R3-C 守卫自检**（皮肤采集非空 + 数量下限，防 `skin/` 清空后 R3-A/R3-B 真空通过） |
| **R4 可摘除** | 真读产物证明「产物 JS 段 = 非皮肤片拼接 + 皮肤片贡献；摘除皮肤 == 非皮肤拼接」+ 合成皮肤替换演练（引擎前后缀逐字节不变） |
| **F1/F2/F3 收尾** | 修正 5 处陈旧活引用（`keys.js`/`highlight.js`/`complete.js`/`library.js`/`struct.js`，纯文件名替换）；R3-B 零容忍集补上全部 `【…】` 完整标记串；新增 R3-C 防真空自检（词元 ≥ 50 / 长语料 ≥ 3）。收尾**并入同一 v7.14**（未推送/未打 tag），不新开 v7.15 |
| **产物** | 230492 → **230515 B**（R2 导出键 +2；F2 5 处注释 +21）；sha256 `19d762a2d63e96e64205d6a9f9bb91b44a4fddf193e172302e5565ccbaad9fa6` |
| **闸门** | `verify_v78` 50 → **54**（+R3-A +R3-B +R3-C +R4）；`node dev/_qa/run-gate.mjs` = **7/7**（83/18/16/**54**/18） |
| **基线** | 新 `dev/_qa/snapshots/BASELINE_v7.14.md`；等价性 `OLD` 默认 → `PHJ_v7.14_20260917.html` |

## v7.8 功能现状（编辑器「结构层 + 候选气泡 + 槽位 + 复制全文」，分支 `feat/editor-blocks`）

| 功能 | 状态 |
|---|---|
| **结构层**：按符号把文本切成「节」（起手式／画面开始／叙事正文／分镜 镜头N／风格包／风格包·属性／硬性要求）；领域语义只活在 `STRUCT_MARKS` 标记表里 | ✅ v7.8 |
| **状态栏「节」**：随光标即时显示当前所在结构 | ✅ v7.8 |
| **候选气泡（两级）**：输入 `#` → **组视图**（数字 1..9 / Enter / Tab / 鼠标 = 进组；本节相关的组置顶）→ **组内视图**（数字 = 直取该条上屏，或 ↑↓ + Enter）；查询态为扁平结果；Esc 逐级退回；Tab 不逃逸焦点；中文态不占键位 | ✅ v7.8 |
| 候选内容：风格包（**内置真实风格包**：全套/5 段/硬性要求；亦可用「导入写作资产」按组并入/替换）｜起手式 4｜结构件 3｜镜头句 8｜景别 8｜运镜 13｜台词 3 —— 参数化槽位，**未用过的词带徽章**（推拉移跟环绕升降甩／远大全景／主观视角）；**素材绑定位一律写 `@`**（不写 `{{node:…}}`，那是画布自动生成的） | ✅ v7.8 / v7.8.2 |
| **槽位 `${n}`**：上屏即被吃掉、光标落位、**Tab 逐位跳**（含槽位内输入的位移同步） | ✅ v7.8（轮盘的前置） |
| **复制全文**：底部 `⧉ 复制全文` + `Ctrl+Shift+C`（送 `textarea.value` 原文，不走渲染层） | ✅ v7.8 |
| **补全配置窗口**：顶栏 `补` → 浏览/搜索/编辑/新增/删除（带撤销）/**拖拽排序（条目换位、跨组拖=改归组、整组换位；拖分组/拖条目时左侧浮现顺序小窗，拖到第 N 行即就位）**；首开把内置表物化成用户表，落 `state.cmpl`（随导出 JSON 走），组顺序落 `cmpl.gorder`（配置窗口与候选气泡共用），改完即生效 | ✅ v7.8 |
| **写作资产 导入 / 导出**（**Q7 资产分离**）：窗口 foot `导入写作资产` / `导出写作资产`——**本机 `.json` 文件、`FileReader`、零网络**；导入=**按组并入**（资产里的组以资产为准、其它组不覆盖），条目打标 `src:'asset'`；导出=当前片段库 → `phj-writing-asset` 格式（可换机/备份）。内置「风格包」组为**真实风格包**（v7.8.2：撤销 v7.8.1「降为中性示例」，风格包即语料的一部分） | ✅ v7.8.1 / v7.8.2 |
| **「恢复内置默认」语义（Q7-7）**：**先确认** → 丢弃内置片段（`src:'seed'`）+ 重注内置片段，**保留** `asset`/`user` 条目（绝不静默删用户导入的资产）；条目 `src ∈ {seed, asset, user}`（缺失视为 `user`） | ✅ v7.8.1 |
| **Esc 顺序**：候选打开时第一次 Esc 只关候选、第二次才关编辑器 | ✅ v7.8 |
| **IME 组合态门控**：拼音未上屏时不弹候选 | ✅ v7.8（合成事件验证；真机手感待复验） |
| **槽位轮盘**（按住槽位 → 扇形展开 → 松开填入；量大多层） | ❌ 未做（第二个 session） |
| 行号栏节标记 / 左侧结构竖条 | ❌ 未做（结构提示只上了状态栏一档） |
| 画布内联编辑的候选 / 「复制并关闭」 | ❌ 未做 |

## v7.7 功能现状（文本编辑器「台词区」豁免）

| 功能 | 状态 |
|---|---|
| **台词区豁免**：被**一行内成对的中文双引号 `“ ”`** 包住的错误符号（`，`/`,`）**一律无视**——随所在层级族色正常显示、**不计入**状态栏错误数、行号**不标红** | ✅ v7.7 |
| 边界＝**仅中文双引号**（半角 `"` 里的 `,`/`，` 照旧标红；`「 」`/`‘ ’` 里的也照旧） | ✅ v7.7 |
| **未闭合不算**台词区：引号没在同一行闭合 → 照旧标红（宁缺勿滥） | ✅ v7.7 |
| **嵌套里层也豁免**：`“外{内，层}层，尾”` 两处逗号都豁免，且各自随所在层级族色（花括号族 / 引号族） | ✅ v7.7 |
| **计数与着色同源**：状态栏错误数由「正则单数」改为复用分类结果（不再出现「颜色放过、计数还报」的分叉） | ✅ v7.7 |
| 无视觉标识 / 无开关（按拍板「完全无视」；边界表 `HL_DLG` 与错误表 `HL_ERR` 皆为文件头常量，改一行即可增减） | ✅ v7.7 |
| 台词区跨行 / 半角引号 / `「 」` 内的豁免 | ❌ 未做（按拍板口径明确排除） |

## v7.6 功能现状（编辑器 coding 化 + 画布块「放大」）

| 功能 | 状态 |
|---|---|
| **通用符号着色**：文本按其中的符号自动分类着色（零领域语义，不预设"某某符号代表什么"） | ✅ v7.6 |
| 规则：成对符号与其内文本同族色 / 嵌套内层覆盖外层 / 其余符号（区域内随族色、区域外着句读色）/ 未闭合着至行尾（换行重置）/ 叙述着专属墨蓝 | ✅ v7.6 |
| **错误符号标红**：中文逗号 `，` 与半角逗号 `,` → 红字 + 波浪下划线 + 行号标红（优先级高于符号族、不参与配对；表 `HL_ERR` 可一行扩展） | ✅ v7.6 |
| **coding 化三项**：等宽字体 / 行号栏（不占文本流、续行不编号）/ 状态栏（行·列·字符·错误数） | ✅ v7.6 |
| **光标处括号配对高亮**（不跨行） | ✅ v7.6 |
| **画布块「放大」按钮**（拖手列 `⤢`，单块语义）→ 打开同一 coding 编辑器，写回画布块 + 同步拼接栏 + 刷光线 + 落盘（不 trim） | ✅ v7.6 |
| **打开编辑器不再默认全选**：光标落文末、无选区（打字是追加而非整段替换）——修掉 v6.12 起的旧行为 | ✅ v7.6.1 |
| 缩进参考线 / Tab 缩进 / 画布内联着色 / 拼接栏着色 | ❌ 未做（按拍板范围与勾选） |

## v7.5 改进（空格平移 + 光线接口 + 文本同步）

| 项 | 状态 |
|---|---|
| **空格 + 左键 = 平移画布**（等价鼠标中键；`spacePan` 标志 + `body.space-pan` 光标反馈；文字编辑/独立窗口/顶栏/拼接栏/菜单区域豁免；`window.blur` 兜底） | ✅ v7.5 |
| **拼接栏条目左缘「渐变朦胧光线接口」**：有画布块光线接入的条目 → `.lnk-in` + `::before`（蓝紫渐变 + blur 3px 光斑）；移出拼接后接口消失 | ✅ v7.5 |
| **拼接栏文本与画布块始终保持一致**：画布内联编辑 → `syncSpliceText()` 定向同步同 id 条目（不重建整栏、不打散动画）+ `updateLinks()` 刷新光线端点；平铺/单元内条目均覆盖 | ✅ v7.5 |
| E 组 17 项断言（**85/85 全绿**，含真实键盘 + 真实鼠标验证）+ 视觉复验（像素分析 + ×5 放大 + 模型描述确认） | ✅ v7.5 |

## v7.4 修复（拼模式虚影错位）

| 项 | 状态 |
|---|---|
| **虚影位置错位修复**：根因 = `cloneNode(true)` 复制原块 inline `left/top`（画布坐标），inline 优先级高于 CSS 的 `left:0` → 虚影基点错用原块坐标（实测偏差 177px）→ `createGhost` 显式归零 `left/top/transform/transition`，虚影贴合光标（偏差 = 抓取偏移，与拖真实块手感一致） | ✅ v7.4 |
| 防复发断言 D2f2（inline 归零 + position:fixed）/ D2f3（虚影位置贴合光标 ≤3px）；诊断工具 `_build/diag_ghost_offset.mjs` | ✅ v7.4 |

## v7.3 优化（两项：缩放位置保持 + 拼模式虚影）

| 项 | 状态 |
|---|---|
| **恢复默认缩放不再重置画布位置**：`resetZoom` 改为**以视野中心为锚**（中心处内容视觉不动、pan 不归零）；缩放已是 100% 时点击无动作（原「视角归零」复合语义取消） | ✅ v7.3 |
| **拼模式虚影拖拽**：拖动文本块 = 拖**半透明虚影**（源块原位不动 + `.splice-src` 标记），**渐变光线实时连接源块↔虚影**（`.lnk-ghost`）；松手落单元才拼入（源块位置始终不变）；空放不拼入且虚影清理；图片块不可拼入仍走普通拖拽；退出拼模式自动清理 | ✅ v7.3 |
| 验收：D 组 19 项新断言（**66/66 全绿**）+ 视觉复验（虚影/光线可见、无异常） | ✅ v7.3 |

## v7.2 修复（拖拽跟手性回归）

| 项 | 状态 |
|---|---|
| **拖拽跟手性回归修复**：根因 = v7.1 为 hover 浮起加的 `.block{transition:transform .15s}` 让拖拽每帧位移被 150ms 缓动（实测滞后 115px/150px）→ 拖拽态禁过渡（CSS `.block.drag` + inline `transition:none` 双保险）+ 落位先 `void offsetWidth` 提交位置再恢复过渡（防回弹） | ✅ v7.2 |
| **光点复用式更新**：`updatePeekDots` 不再每帧重建 DOM（新出现才建元素 + 播 dotIn；既有光点只更新位置）——消除拖拽/平移中反复重播入场动画与 DOM 开销 | ✅ v7.2 |
| 跟手量化断言（C9）+ 多场景回归工具 `_build/diag_drag_lag2.mjs`（单块/甩动/组拖/缩放，滞后 2~4px） | ✅ v7.2 |

## v7.1 功能现状（动效微交互层 · P1 五项 + P2 tick）

| 功能 | 状态 |
|---|---|
| **按钮按压**：顶栏 / 块操作 / 拼接栏 / 菜单等按钮 `:active` scale .96/.88（90ms；原有背景/颜色过渡完整保留） | ✅ v7.1 |
| **块 hover 浮起**：鼠标移到画布块 → translateY(-1px) + 阴影加深（150ms） | ✅ v7.1 |
| **拖拽中 lift**：拖动 / 组拖时块 scale(1.02)（跟手即时无缓动——为保跟手性的取舍） | ✅ v7.1 |
| **选中描边淡入**：`.block.selected` 由 outline 改 box-shadow（可过渡），选中/取消 150ms 淡入 | ✅ v7.1 |
| **drop-target 光晕呼吸**：拼模式高亮单元 1.9s 无限呼吸循环（幅度克制） | ✅ v7.1 |
| **缩放值数字 tick**：顶栏缩放值变化 140ms 淡入（动画期间不重播，连续滚轮不闪） | ✅ v7.1 |
| **拖拽启动清理（健壮性）**：清 FLIP 残留 inline transition/delay + 移除 pop-in/suck 动画类——修复「整理/拼入动画中立即拖块」的跟手性 | ✅ v7.1 |
| P2#15 背景光斑漂移 / P2#17 缩放平滑 | ⛔ 技术判断不做（毛玻璃全屏重算性能 / 跟手红线；理由见发布说明 v7.1 段） |

## v7.0 功能现状（动效 · P0 九项）

| 功能 | 状态 |
|---|---|
| **块创建弹入**：双击建块 / 粘贴建块（文本·图片）/ 克隆 → scale .94→1 + 上浮（160ms decelerate） | ✅ v7.0 |
| **块删除收拢**：先播 110ms 收拢淡出再真正移除（撤销快照动画前拍，批量撤销保持） | ✅ v7.0 |
| **一键整理 FLIP**：块从旧位平滑飞抵新位（320ms premium ease-out），错峰 8ms/块，动画后清 inline 样式 | ✅ v7.0 |
| **拼入吸入 + 条目弹入**：松手拼入画布块微缩「吸入」（170ms）+ 拼接栏新增条目弹入落定（160ms） | ✅ v7.0 |
| **拼入光线生长**：新增连线 pathLength 归一化 dash 1→0 生长（240ms）；常态刷新静态不闪 | ✅ v7.0 |
| **拖手光点淡入**：被遮挡块光点出现即淡入（120ms） | ✅ v7.0 |
| **窗口弹开**：遮罩淡入（180ms）+ 窗口 scale/上浮弹入（240ms）；关闭保持即时（Esc 优先） | ✅ v7.0 |
| **菜单小弹入**：右键菜单 / 「导」下拉 translateY -4px + 淡入（120ms） | ✅ v7.0 |
| **toast 滑入滑出**：滑入 200ms；2.6s 后 150ms 滑出再隐藏 | ✅ v7.0 |
| **无障碍降级**：prefers-reduced-motion → 全部动效直切（功能不变，无 UI 开关） | ✅ v7.0 |
| 红线保持：拖拽/平移/缩放跟手 0 延迟、无新 UI 控件、数据模型零改动（version 12） | ✅ v7.0 |
| ~~P1 微交互 / P2 氛围~~ | ✅ v7.1 已交付（见上表；P2#15 背景漂移 / P2#17 缩放平滑经技术判断跳过） |

## v6.21 功能现状

| 功能 | 状态 |
|---|---|
| **移除底部说明小字**：画面底部 .hint 提示行已移除（样式保留备用） | ✅ v6.21 |
| **复制拼接空行规则重定义**：提示词块不参与空行加入（块间无空行）；**前缀值段上方加一个空行**（加在前缀段前，前缀不在开头时同样生效）；后缀值不参与（值间无空行）；平铺块之间也无空行；其余全部 `\n` 紧挨连接 | ✅ v6.21 |

## v6.20 功能现状

| 功能 | 状态 |
|---|---|
| **块编辑窗口换行显示**：编辑区 white-space:pre-wrap + overflow-wrap:break-word——单行超长文本在窗口内自动折行显示（不再横向滚动）；窗口宽度固定 640px（min 94vw），不再按最长行撑宽 | ✅ v6.20 |
| **复制输出值间空行**：复制拼接时前缀值们之间、后缀值们之间用空行分隔（值1\\n\\n值2，与块间一致） | ✅ v6.20 |

## v6.19 功能现状

| 功能 | 状态 |
|---|---|
| **窗口内点击豁免（核心修复）**：onMouseDown 空白平移分支排除 `.modal-mask`——点击独立窗口内元素不再 blur 焦点（块编辑窗口输入框可正常聚焦打字）也不再启动画布平移（模板窗口单元拖拽不被 panning 吞掉，HTML5 DnD 正常触发） | ✅ v6.19 |
| **独立窗口焦点管理**：模板定制/模板预览窗口打开时 blurActive 抢走画布焦点 + 聚焦窗口容器（tabindex=-1）；块编辑窗口/命名模态框保持聚焦窗口内输入框 | ✅ v6.19 |
| **方向键全局豁免**：任意 modal-mask（编辑/定制/预览/命名）打开时方向键不动画布（焦点兜底，双保险） | ✅ v6.19 |

## v6.18 功能现状

| 功能 | 状态 |
|---|---|
| **方向键多键冲突修复**：keyState 逐键记录真实按下状态 + keyDir 派生——快速连按/同轴反向（→ 与 ← 短暂同按）不再误清方向卡死；同轴反向抵消后松开一方自动恢复方向并重启循环 | ✅ v6.18 |
| **方向键 ÷zoom**：位移除以缩放值——高缩放时视觉移动速度恒定（不再异常快） | ✅ v6.18 |
| **缩放后强制重绘**：zoomAt/resetZoom 后 fixTextBlur()（.rf will-change 触发重绘）——修复放大后提示词块文字模糊（transform scale 光栅化滞留） | ✅ v6.18 |
| **拖画布中 Ctrl+滚轮缩放**：左键拖动画布（panning）过程中 Ctrl+滚轮照常缩放（原有逻辑验证可用） | ✅ v6.18 |
| **模板单元拖拽排序**：模板定制窗口单元卡片 draggable，拖动改变单元顺序（拖到目标卡片位置插入），标题区「≡ 拖动排序」提示 | ✅ v6.18 |
| **模板套用入口优化**：套用按钮主题色主操作；新增「预览」按钮 → 只读预览窗口（模板序号标题 + 每单元前缀/块容器/后缀结构） | ✅ v6.18 |
| **拼接栏单元序号**：单元标题不再显示模板名称「模板 N」，按拼接栏内序号显示「单元 1」「单元 2」 | ✅ v6.18 |
| **拼模式**：拼接栏「拼」按钮 toggle——开启后直接拖动任意提示词块到任意单元，松手即拼入该单元（拖拽中单元高亮 drop-target）；再按「拼」或 Esc 退出；非拼模式拖动不拼入 | ✅ v6.18 |

## v6.17 功能现状

| 功能 | 状态 |
|---|---|
| **方向键平滑移动**：LOL 式——按住加速曲线（0→900px/s，约 0.21s 达速）、松手即停、支持对角线；镜头语义方向修正（按 → 视角右移、内容左移）；输入场景豁免 | ✅ v6.17 |
| **图片块无拖手**：完全无边框纯图片；直接按住图片拖动（mousedown 在 img 上即启动拖拽）；删按钮回位图片左下角（left:8）；被遮挡时光点居图片中心 | ✅ v6.17 |
| **中键全局平移**：鼠标中键按下（任意位置，含块上）即开始平移视角，拖动跟手；阻止浏览器 autoscroll | ✅ v6.17 |
| **遮挡关系复查**：最近被点击/编辑过的块置顶（`.active` z 6000，render 重建后恢复）；遮挡判定按有效层级 peek>drag>active>普通（同层 DOM 靠后者在上）而非纯几何；active 块不被低层块遮挡（无光点），可正常操作 | ✅ v6.17 |

## v6.16 功能现状

| 功能 | 状态 |
|---|---|
| **拖手光点**：拖手被其他块遮挡时，拖手位置显示朦胧渐变光点（蓝→紫 radial 渐变）；悬停光点 → 对应块临时置顶（z 9900）拖手露出可拖；鼠标离开恢复原层级 | ✅ v6.16 |
| **拼接光线**：块拼入拼接栏后，画布块右缘 → 拼接栏条目的朦胧渐变贝塞尔光线（两端小圆点）；拼接栏内编辑块文本光线依旧连接；移出/清空拼接光线消失；拖动/平移/缩放时光线跟随 | ✅ v6.16 |
| **顶栏简化**：整理 →「整」；导出/导入收纳进「导」下拉（复用右键菜单样式，点击 toggle，导出 JSON 自动剔除图片块） | ✅ v6.16 |
| **prompt 块无边框窗口**：除左侧拖手外，块 border=0、阴影极淡、背景降透明（hover 轻微浮起保持可感知） | ✅ v6.16 |
| **图片块**：Ctrl+V 粘贴剪贴板图片 → 图片块（按原始分辨率自适应窗口）；无边框，删按钮在图片区域左下角 hover 浮现（left:36 避开拖手）；仅展示不可拼入/克隆/编辑；右键菜单仅「删除」；**仅会话内存在（不落盘不导出，刷新/重开消失）** | ✅ v6.16 |
| **方向键移动画布**：↑↓←→ 平移视角 40px/次（按住自动 repeat）；输入场景（textarea/input）豁免保留光标语义 | ✅ v6.16 |
| 数据模型 version 12：图片块仅内存态，saveNow/导出用 sanitizeState 剔除；migrate 防御剔除意外图片块（在 map 前过滤） | ✅ v6.16 |

## v6.15 功能现状

| 功能 | 状态 |
|---|---|
| **一键移除空行**：画布块右键菜单「移除空行」+ 块编辑窗口左下角按钮；按行过滤空白行重拼（`trim()!==''`）；多选批量（菜单显示「移除空行（N 块）」）；无空行时提示不改文本 | ✅ v6.15 |
| **块按钮简化**：右侧按钮列移除；左侧拖手 = 顶部「拼」（红色常显）+ 中部 6 圆点拖区 + 底部「删」（弱化色 hover 变红）；拖拽只从中部拖点启动 | ✅ v6.15 |
| **复制/克隆收进右键菜单**：画布块右键菜单 = 拼入拼接/复制/克隆/移除空行/删除（textarea 内保留原生菜单） | ✅ v6.15 |
| **修复删除 bug**：v6.6 重构丢失 blocks 删除（点「删」仅移出拼接、画布块删不掉）已修复，单块/批量删除均真正删块（批量撤销保持） | ✅ v6.15 |

## v6.14 功能现状

| 功能 | 状态 |
|---|---|
| **块编辑窗口大尺寸**：编辑区高度 = 屏幕高度 80%（窗口不超屏）；宽度自适应最长字行（输入实时重测，下限 560px 上限 94vw）——长提示词视野开阔 | ✅ v6.14 |

## v6.13 功能现状

| 功能 | 状态 |
|---|---|
| **模板 = 任意数量单元（序号区分）**：模板名称移除，显示/选择/单元窗口名用「模板 N」；单元 = 前缀值 + 后缀值（定制窗口仅配置这两个），中间为拼入块的容器 | ✅ v6.13 |
| **块不属于模板**：套用 = 模板全部单元按序生成（前缀+空块区+后缀），画布块拼入填充；拼接栏块点击弹编辑窗口（同步画布）保留 | ✅ v6.13 |

## v6.12 功能现状

| 功能 | 状态 |
|---|---|
| **模板 = 任意数量「模板单元」**：单元 = 前缀值 + 任意数量提示词块 + 后缀值（从上到下排列）；定制窗口主体 = 模板名称 + 全部单元卡片 | ✅ v6.12 |
| **打开定制窗口即新建模板**（无模板自动创建，自动带 1 空单元）；「＋新建模板」按钮移除 →「＋新建单元」；新建模板入口收进选择器下拉「＋ 新建模板…」 | ✅ v6.12 |
| **提示词块独立编辑窗口**（点击任意块弹出，风格统一）：模板单元块 / 拼接栏内嵌块 / 拼接栏画布引用块（编辑同步画布） | ✅ v6.12 |
| **套用 = 替换 + 全部单元按序生成**：块文本进 blockTexts（不建画布块），拼入目标=第一个单元；复制输出 = 前缀+（内嵌+引用按序）+后缀 | ✅ v6.12 |

## v6.11 功能现状

| 功能 | 状态 |
|---|---|
| **套用模板 = 追加单元**：同一模板可套用任意多次（如「对话场景」多段并存），各单元独立拼入内容；激活单元「拼入到此」标记 + 点击窗口头切换目标；复制输出按单元顺序拼接；「清空」按钮承担替换清空语义 | ✅ v6.11 |

## v6.10 功能现状

| 功能 | 状态 |
|---|---|
| **「拼」按钮始终显式 + 红色**：块右侧红色「拼」常驻显示（不用 hover 直接可点），醒目主操作；hover 块时复制/克隆/删除照常浮现（极简 hover 模式不变） | ✅ v6.10 |

## v6.9 功能现状

| 功能 | 状态 |
|---|---|
| **Ctrl+滚轮缩放画布**：以鼠标指向处为基准（光标下内容不动），范围 25%~400%，每格 ×1.1；缩放只影响显示，块坐标/拼接栏/模板不受影响；缩放值持久化（重启保持） | ✅ v6.9 |
| **顶栏缩放按钮**：实时显示当前缩放值（如 110%）；点击 = 恢复默认（zoom→1 + 视角归零），toast 提示 | ✅ v6.9 |
| **坐标换算适配**：拖块/平移/双击建块/Ctrl+V/滚轮滚动全部按画布坐标换算（client ÷zoom）；拖拽中 Ctrl+滚轮或滚轮滚动时块自动补偿保持相对鼠标（取代 v6.2 wheelCompY 手写补偿） | ✅ v6.9 |

## v6.8 功能现状

| 功能 | 状态 |
|---|---|
| **移除右键拖拽拼入手势**：右键拖把手不再拼入/不再跟随（浏览器鼠标手势劫持右键划动，页面无法阻止，用户拍板移除）；右键=纯菜单语义——单击块弹操作菜单（拼入/复制/克隆/删除），textarea 内保留原生菜单 | ✅ v6.8 |
| **拼入入口收敛**：块上 hover「拼」按钮 / 右键菜单「拼入拼接」/ 多选批量拼入（进当前激活单元语义不变）；拼接栏空态文案与示例块提示同步更新 | ✅ v6.8 |

## v6.7 功能现状

| 功能 | 状态 |
|---|---|
| **模板定制 = 单模板工作模式**：窗口每次只定制一个模板，其中创建的（前缀值/后缀值）皆为**同一模板**的组成部分；顶部模板选择器切换当前编辑对象；「＋ 新建模板」创建新模板并自动切换 | ✅ v6.7 |

## v6.6 功能现状

| 功能 | 状态 |
|---|---|
| **模板定制窗口仅配置前缀/后缀值**（内容块与载入拼接已移除）；模板 = {name, prefixes[], suffixes[]} | ✅ v6.6 |
| **套用模板 → 套用进拼接栏**：清空现有拼接，创建模板单元圆角窗口（不建画布块）；新单元自动成为「拼入到此」目标 | ✅ v6.6 |
| **拼接栏宽度加倍（560px）+ 条目化**：平铺块条目 + 模板单元窗口条目（窗口 = 标题/前缀标签行/内部 prompt 块圆角小窗/后缀标签行）；无单元时拼入 = 平铺块条目（依旧平铺） | ✅ v6.6 |
| 复制拼接：按条目顺序输出（单元 = 前缀们 + 块们 + 后缀们，值独立行、块间空行、条目间空行） | ✅ v6.6 |
| 数据迁移 v8：旧 order 平铺 → 块条目；旧模板 items 弃用（保留前后缀） | ✅ v6.6 |

## v6.5 功能现状

| 功能 | 状态 |
|---|---|
| **模板定制窗口**：拼接栏「⚙ 模板定制」→ 居中弹窗（风格统一、固定高度内部滚动）——新建任意数量模板单元；每单元可配置任意数量前缀值/后缀值（标签式增删、每值独立一行）、内容块多行文本区（空行分隔=多块）、「载入当前拼接」快捷填充；窗口内可删除单元 | ✅ v6.5 |
| 移除「＋存为模板」（模板改由定制窗口管理）；模板列表保留套用/删除（删除可撤销） | ✅ v6.5 |

## v6.4 功能现状

| 功能 | 状态 |
|---|---|
| **Ctrl+V 粘贴建块**：焦点不在输入区时，Ctrl+V 将剪贴板文本整段创建为新块（视口中心错位摆放；textarea/模态框内保留原生粘贴） | ✅ v6.4 |
| **模板系统重构**：模板单元 = 前缀值 + 内容块列表 + 后缀值（前缀/后缀可空，输出时各自独占一行、块间空行分隔）；存模板/改名模态框多字段化；旧模板自动补空前后缀 | ✅ v6.4 |

## v6.3 功能现状

| 功能 | 状态 |
|---|---|
| **Ctrl+左键点选多选**：按住 Ctrl 左键点块 = toggle（已选则取消）；任意位置（把手/卡片/textarea）不启动拖拽/编辑；点空白清空 | ✅ v6.3 |
| **右键菜单不修改选择集**：右键块在选中集内 → 菜单批量作用于选中集（批量标签）；否则单块；Ctrl+右键多选已放弃（用户拍板） | ✅ v6.3 |

## v6.2 功能现状

| 功能 | 状态 |
|---|---|
| **Ctrl+右键多选（可靠版）**：mousedown 层处理——把手轻移/右键拖拽拼入后/textarea 任意位置均不被吞；累加选择+高亮+批量操作（v6.1 语义保持） | ✅ v6.2 |
| **拖块中滚轮**：拖动 prompt 块时滚轮上下滑动画布，拖拽块保持跟随鼠标（滚轮补偿并入松手落点；组拖同步） | ✅ v6.2 |

## v6.1 功能现状

| 功能 | 状态 |
|---|---|
| **画布移动粘滞感**：拖空白平移带平滑阻尼跟随 + 松手轻惯性滑行（不越过终点） | ✅ v6.1 |
| **Ctrl+右键多选**：累加选择、选中高亮；选中集=「新单体」——拖把手组拖、右键菜单与块按钮批量拼入/复制/克隆/删除（批量删除可撤销） | ✅ v6.1 |
| 空文本框无默认文本（placeholder 已移除） | ✅ v6.1 |
| 无限画布（无滚动条 / 坐标不限 / 拖空白平移 / pan 持久化） | ✅ |
| **左键双击画布空白处新增块**（落在双击处；右键菜单「在此处新增块」仍可用） | ✅ v6 |
| **滚轮上下滑动画布**（纵向平移视角，防抖落盘） | ✅ v6 |
| **一键整理**：所有块从上到下左对齐排列（按 y 排序，x=20，块高+24 间距，视角归零） | ✅ v6 |
| **块宽自适应最长行**：文本不自动换行，一整行完整显示（canvas 测量，实时重测；下限 140px） | ✅ v6 |
| 块编辑：原生 textarea，回车/Backspace 全原生，高度随内容自动长高全文展开 | ✅ |
| 重叠时把手 z-index 9999 置顶可见可操作 | ✅ |
| 单块复制 / 克隆（错位 +28）/ 删除 | ✅ |
| **拼接栏模板**：多预设单元，每单元容纳多 prompt 块；存为模板（命名模态框）/ 套用=替换当前拼接 / 改名 / 删除（toast 撤销） | ✅ v6 |
| 拼接栏：拖入/按钮收集/拖拽排序/复制拼接（空行分隔） | ✅ |
| localStorage 防抖自动保存 + 双兜底 flush | ✅ |
| 导出 / 导入 JSON（导入前备份、可撤销） | ✅ |
| v1/v2/v3/v4 旧数据自动迁移（version 5；templates 补空数组；order 过滤失效 id） | ✅ |
| 边缘滚动（鼠标贴边自动平移） | ❌ 已移除（v6 拍板，滚轮取代） |
| 「＋新增块」按钮 | ❌ 已移除（v6 拍板，双击取代） |

---

## 附：设计演进（v1 → v7.7，共 31 段）

<details>
<summary><b>设计演进（v1 → v7.7，共 31 段，点击展开）</b></summary>

- v1 垂直列表
- v2 自由画布
- v3 无限画布+把手置顶
- **v3(final) 移除复制全部、回车分段回退为原生 textarea（2026-09-07 用户拍板）**
- **M3 发布（2026-09-08 验收 20/20 全绿）**
- **v4 拼接侧边栏（2026-09-08）**
- **v5 侧边栏独立悬浮 / 空行分隔 / macOS 浅色视觉**
- **v5.1~v5.4 系列迭代（毛玻璃显性化/自定义右键菜单/拼入手势/更名拼好镜/边缘滚动）**
- **v6.0（2026-09-08）移除边缘滚动 / 文件更名 PHJ.html / 双击空白新增块 / 滚轮上下滑动 / 一键整理 / 块宽自适应最长行 / 拼接栏模板（套用=替换）**
- **v6.1（2026-09-09）画布移动粘滞感（阻尼+惯性）/ 移除空文本框默认文本 / Ctrl+右键多选（选中集=新单体，批量操作全可用）**
- **v6.2（2026-09-09）Ctrl+右键多选可靠性修复（mousedown 层处理）/ 拖块中滚轮：画布滚动且块跟随鼠标**
- **v6.3（2026-09-09）放弃 Ctrl+右键多选（用户拍板）/ 新增 Ctrl+左键点选多选（toggle）**
- **v6.4（2026-09-09）Ctrl+V 粘贴剪贴板文本建块 / 模板系统重构：模板单元=前缀值+内容块列表+后缀值（可空，输出独占行）**
- **v6.5（2026-09-09）移除「存为模板」/ 新增「模板定制」居中窗口（多模板单元、每单元任意数量前缀/后缀值、内容块文本区、载入当前拼接）**
- **v6.6（2026-09-09）模板定制仅配置前后缀 / 套用进拼接栏（不建画布块）/ 拼接栏宽度加倍（560px）并条目化：模板单元圆角窗口+内部 prompt 块圆角小窗 / 拼入进激活单元 / 旧数据平铺迁移**
- **v6.7（2026-09-09）模板定制窗口单模板工作模式：每次定制一个模板，窗口内创建的（前缀/后缀值）皆为同一模板的组成部分；顶部选择器切换 + 「＋ 新建模板」**
- **v6.8（2026-09-09）移除右键拖拽拼入手势（浏览器鼠标手势劫持右键划动，跨设备不可靠）：右键=纯菜单语义，拼入统一走块上「拼」按钮/右键菜单「拼入拼接」/批量拼入**
- **v6.9（2026-09-09）画布缩放：Ctrl+滚轮以光标为基准缩放（25%~400%），顶栏按钮显示缩放值+点击恢复默认（缩放+视角归零）；全部鼠标交互坐标换算适配缩放（拖块/平移/双击建块/Ctrl+V/滚轮滚动按 /zoom 换算，拖拽中缩放/滚轮自动补偿保持块跟鼠标）**
- **v6.10（2026-09-09）「拼」按钮始终显式+改红色（拼入主入口常驻醒目，visibility 方案只常显拼、复制/克隆/删除保持 hover 浮现）**
- **v6.11（2026-09-09）套用模板 = 追加单元：同一模板可创建任意数量单元（替换清空由「清空」按钮承担），拼入目标自动切到新单元，多单元按序输出**
- **v6.12（2026-09-09）模板结构正本清源：模板=任意数量「模板单元」（单元=前缀值+任意数量提示词块+后缀值）；打开定制窗口即新建模板（无模板自动创建），「＋新建模板」移除改为「＋新建单元」，新建模板入口收进选择器下拉；提示词块点击弹独立编辑窗口（模板定制与拼接栏通用，画布块编辑同步）；套用=替换+全部单元按序生成（内嵌块 blockTexts 不建画布块）；数据模型 version 10**
- **v6.13（2026-09-09）模板正名：移除模板名称（序号「模板 N」区分）；移除模板定制窗口的提示词块配置——块不是模板的一部分，单元=仅前缀+后缀、中间为拼入容器（套用后块区空态，拼入填充）；数据模型 version 11**
- **v6.14（2026-09-09）块编辑窗口：高度 = 屏幕高度 80%；宽度自适应最长字行（canvas 测量实时重测，下限 560px 上限 94vw）**
- **v6.15（2026-09-10）一键移除空行（画布块右键菜单 + 编辑窗口按钮，多选批量）；块功能按钮简化为拖手顶部「拼」（红色常显）+ 底部「删」（弱化防误触），复制/克隆收进右键菜单，右侧按钮列移除；修复 v6.6 重构丢失的画布块删除（此前「删除」仅移出拼接引用，块永远留在画布）**
- **v6.16（2026-09-10）拖手被遮挡时朦胧渐变光点（hover 临时置顶可拖，离开恢复）；拼入光线（画布块↔拼接栏条目，编辑后仍连）；顶栏简化（整/导，导入导出收纳「导」下拉）；prompt 块无边框窗口；图片块（Ctrl+V 粘贴剪贴板图片、按分辨率自适应、仅展示+删按钮左下角 hover 浮现、仅会话内不落盘）；方向键移动画布；数据模型 version 12**
- **v6.17（2026-09-10）方向键平滑移动（LOL 式加速曲线、松手即停）+ 镜头语义方向修正（按
- 视角右移内容左移）；图片块取消拖手完全无边框，直接按图拖动；鼠标中键任意位置拖动 = 全局平移视角；块遮挡关系复查——最近被点击/编辑过的块置顶（active z 6000），遮挡判定按有效层级（peek>drag>active>普通），图片块光点居中心**
- **v6.18（2026-09-10）方向键多键冲突修复（keyState 逐键 + keyDir 派生，快速连按不卡死）+ ÷zoom 视觉速度恒定；缩放后强制重绘修复文字模糊；模板单元拖拽排序；模板套用入口优化 + 只读预览；拼接栏单元按序号「单元 N」；拼模式（拖提示词块到任意单元）**
- **v6.19（2026-09-10）独立窗口焦点修复：模板定制/预览窗口打开时抢走画布焦点（blurActive + 窗口容器 tabindex=-1 聚焦），打字/方向键不再作用于背景画布；方向键全局豁免（任意 modal-mask 打开不动画布）；修复 onMouseDown 空白平移分支未豁免独立窗口（点击窗口内元素抢焦点/吞拖拽）**
- **v6.20（2026-09-10）块编辑窗口单行文本允许换行显示（pre-wrap + 断词折行，宽度固定 640 不再按最长行撑宽）；复制拼接输出时前缀值们/后缀值们之间用空行分隔（与块间一致）**
- **v6.21（2026-09-10）移除画面底部说明小字；复制拼接空行规则重定义（用户 clarify 确认「完全无空行」+「平铺块间也去掉空行」）——块不参与空行加入（块间无空行）、前缀值上方加一个空行、后缀值不参与；整段输出只有开头一个空行其余全部 \n 紧挨连接**

</details>
