# IRON_RULES · 项目铁律（拼好镜 PHJ）

> **效力**：本文件是项目的**最高约束**。与 `AGENTS.md`、`HANDOVER.md`、`dev/docs/**` 冲突时，**以本文件为准**。
> **性质**：**永久生效** · **活文件**（可随时改写条文、新增条文）· 随 git 同仓同版，跨平台可读。
> **权限与修订**（2026-09-20 Holly 定）：
> - **`A` 区（根本原则）不可改写**——它是本项目的立身之本；确需变更须由 Holly **明确指示**。
> - **`P` / `E` / `C` / `T` 各区皆可改写、可新增、可移除**——判断标准只有一个：**是否有利于项目的健康发展**（须在变更记录中写明理由及其对项目健康的影响）。
> - **程序**：任何 agent 只能**提议**（在回报中写「建议立为铁律」「建议改写 X 条」）；**改动权归 Holly**，改完须在本文件末尾变更记录追加一行。
> - **禁止**：为"改起来方便""让闸门变绿""少做点事"而改写铁律——那是**弱化，不是修订**。修订的方向永远是**让项目更健康**（更强的可移植性、更清晰的边界、更少的隐性坑），而不是更松。
> **编号**：`A` 根本原则（最高优先）/ `P` 产品红线 / `E` 工程不变式 / `C` 协作铁律 / `T` 测试铁律。
> ★ **引用约定**：任务书、回报、评审一律**引用编号**（如「须守 P1、E2、T7」），不复述条文——
> 条文可改写，**编号永不变**，历史引用因此永不失效。
> **变更记录**：见文件末尾（append-only，只追加不删改），记录每次改写/新增的日期与授权。

---

## A · 根本原则（最高优先 · 一切决策的最终判据）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **A1** | **可移植性与用户零摩擦**——项目必须能在任何机器、任何操作系统、任何 agent 管理区之间自由迁移而不失能，且对使用者保持零摩擦：<br>① **用户侧零摩擦**：**打开即用**——不要求用户安装、不要求配置、不驻留后台进程、不污染用户系统（这是产品承诺，**与"用什么形态实现"无关**）；<br>② **跨平台可运行**（Windows ↔ Linux 自由巡回；路径/行尾/浏览器探测/flags 全部平台中立）；<br>③ **不绑定任何机器特定事实**——不写死绝对路径、不依赖某台机器的工具链位置、不依赖 `AppData`/`D:` 盘等本机约定；<br>④ **决策闸门**：任何新依赖、新工具、新机制落地前，必须先回答 **「换到另一台机器 / 另一个 OS / 另一个 agent 区，它还能跑吗？」**——答不上来就不落地；<br>⑤ ★**形态不设锁**：**「单文件 `PHJ.html`」只是当前实现形态，不是本项目的铁律**。当出现能让项目**迈入更优质阶段**的架构方案（模块化分发、构建工具链演进、多资源组织…）时，**不得以「必须单文件」为由否决**——正确做法是评估该方案对 ①–④ 的影响后交由 Holly 决策。<br>**一句话：可移植性与零摩擦是目的，「单文件」只是曾经的手段。** | 项目初衷 + 2026-09-20 可移植性专项；**2026-09-20 Holly 修订：终止强调「单文件」**（理由：不让形态约束使项目错失迈入更优质阶段的重构契机） |
| **A2** | **可灵活交接性**——项目必须能在任意时刻交给任意 agent 而不丢状态：<br>① **知识全部在仓库内**（git 跟踪），**禁止只写进会话记忆**；<br>② 交接入口固定：`IRON_RULES.md` → `HANDOVER.md` → `PROJECT_MEMORY.md` → 环境探测 → **跑闸门 15/15**；<br>③ `HANDOVER.md` 是**活文件**，每次交付必须更新（版本/指纹/在途/待裁决/已巡回平台）；<br>④ 任务书与回报**必须存档**到 `dev/_qa/records/handover/`；<br>⑤ **磁盘状态是唯一可信进度**（会话可随时被中断回收）；<br>⑥ 交接闭环判据：`git status 干净 + 闸门 15/15 + HANDOVER 与实际一致`。 | 2026-09-20 多 agent 协作机制 |

## P · 产品红线（违背即产品作废）

> **P1 已移除**（2026-09-20，Holly 指令）——其实质内容**并入 A1**（单文件 `file://` 即用 / 零安装零进程零残留 / 零依赖），编号空缺保留不改，以免历史引用失效。
> **P5 已移除**（2026-09-20，Holly 指令）——皮肤边界与语料约束由 **E5/E6** 及断言 `R3-A/R3-B/R4` 守门。

| 编号 | 铁律 | 来源 |
|---|---|---|
| ~~P1~~ | **（已移除 → 见 A1）** | — |
| **P2** | 经典脚本：**禁 `type=module`、禁动态 `import()`**（`file://` 下被 CORS 挡）；构建期**零依赖**（纯 node）。<br>★**前提标注（2026-09-20）**：本条服务于「`file://` 直接打开」这一分发形态；若形态演进（见 **A1⑤**），本条须**一并复核**——它属可改写区，修订标准 = 有利于项目健康发展 | 项目初衷 + 形态前提标注 |
| **P3** | `localStorage` 键 `storyboard-prompt-panel:v1` **不变**；`state.version` **只进不退**；`migrate()` 必须**零丢失**（老数据升级后用户看到的东西与升级前一致） | v7.16/v7.18 迁移事故预防 |
| **P4** | **`prefers-reduced-motion` 降级必须保留**——所有动效在 reduce 下直切，功能不得因此受损 | v7.19/v7.20 实现约束 |
| ~~P5~~ | **（已移除 → 见 E5/E6）** | — |
| **P6** | **资产边界与公开仓库**：本仓库 `github.com/Holly-cloud/pinhaojing` **是公开的**。① 私有写作资产（`写作资产*`，含个人人名/台词类文本）**一律不入库**——生成到仓库外 `AA_Dev/_assets/`，`.gitignore` 保险丝在位；② **任何 `git add` / push 之前先检查**：新增文件与提交内容是否含私有资产或真实人名语料（**push 即公开，且历史删除成本极高**）；③ 画布语料 `dev/_qa/records/corpus-20260913/` 属 v7.8 证据链，**Holly 已接受其公开**（勿误删，也勿再新增同类内容）；④ agent 不得把用户真实语料/资产写进文档、测试夹具或断言。 | **2026-09-20 审计补录**（源自 v7.8.1 仓库暴露事件 + Holly 处置） |

## E · 工程不变式（违背即构建/交接断裂）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **E1** | **改产物字节只走合法路径**：`node dev/build.mjs` → 拷新快照到 `dev/_qa/snapshots/PHJ_v<版本>_<日期>.html` → 改 `verify_build_equivalence.mjs` 的 `OLD` → 改 `run-gate.mjs` 头注释 → 闸门全绿 | v7.8 起的收口惯例 |
| **E2** | 唯一真闸门是**等价性逐字节**（`run-gate` 无体积判定式，"体积新值"只改注释） | 实测确认 |
| **E3** | **`state.blocks` 恒为「当前项目的块」**（活动项目槽的镜像引用）；切项目**换引用**不换语义——消费方零改动；`syncActiveProject()` 必须在 `saveNow()` 与 `switchProject()` 两处兜底（防 `filter()` 重赋值解链） | v7.18 多项目设计 |
| **E4** | **顺序敏感**：① `addEventListener` 注册序 ② 顶层 `var` 初始化序（`manifest.mjs` 是 `SLICES` 顺序唯一源；`deps` 是运行期依赖，与文件序无关） | 架构不变式 |
| **E5** | **皮肤边界 5 条守门**（R1 双向不引用 / R3-A 棘轮 ≤15 / R3-B 零泄漏 / R3-C 防真空 / R4 可摘除）；`skin/corpus` 固定在 `SLICES` **第 9 位**；`SLICES` 共 20 条 | v7.13/v7.14 |
| **E6** | **新领域文案落 `view/**` 或 `index.html`**，**禁止落 `skin/`**（会新增词元顶穿 R3-A 棘轮）；改完必跑 `node dev/_qa/lib/skin-guard.mjs` | v7.13 R3-A 顶格事故 |
| **E7** | **不新增任何 `document`/`window` 级 `keydown\|keyup\|blur` 监听**（守 `P2-A`/`P3-A`）；新监听一律元素级或复用既有 `DOMContentLoaded` | P2-A 契约 |
| **E8** | 改 `PHJ` 对外面（加导出名）须同步 `verify_v78.mjs` 的 `P2_EXPORTS_GOLDEN`；**判定式与条数不许改** | P2-B2 契约 |
| **E9** | 源码注释**禁 `` $` `` / `$'` / `$&` / `$n`**（`test-artifact.mjs` 拼接产物时会被当替换模式 → 产物静默拼坏 → 报 `state is not defined`，完全不指向根因） | v7.18 实测事故 |
| **E10** | **本机 git 的 ref 可能静默失败**：含 `/` 的 ref 操作（`git branch feat/x`、`update-ref refs/remotes/...`）**可能 exit 0 却不建/不更新** → 一律用 `git show-ref` / `for-each-ref` 复核；★判断「远端同步到哪」**必须看 push 输出或 reflog**，不能只读 `origin/master`（本机曾长期失真，误判"有 30+ 提交未推"） | 2026-09-20 实测（交接判断可靠性直接相关） |

## C · 协作铁律（违背即交接失效）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **C1** | **仓库即协作总线**：项目知识必须落在 git 仓库内；**禁止只写进会话记忆**（会话会蒸发、换区即丢） | 2026-09-20 可移植性专项 |
| **C2** | **闸门 15/15 全绿 = 唯一的"工作正常"定义**；交接、续作、收尾一律以闸门为准，不以任何口述为准 | 同上 |
| **C3** | **磁盘状态是唯一可信进度**：会话可能随时被中断/回收 → 恢复后先查 `git status` + mtime，判断续做还是重做 | 工程师两度中断事故 |
| **C4** | **跨成员信息流必须经主理人中转**；主理人是**唯一 commit/push 人**；成员任务书必含「不 commit/push」纪律 | 团队 SOP |
| **C5** | 交接闭环判据：`git status 干净 + 闸门 15/15 + HANDOVER.md 与实际一致`——**三者齐才算交接完成** | 2026-09-20 协议 |
| **C6** | 交接/交付时：**任务书与回报必须存档**到 `dev/_qa/records/handover/`（新 agent 靠它考古"为什么"） | 同上 |

## T · 测试铁律（违背即埋雷；全部为实战事故提炼）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **T1** | 断言必须**真读**被测对象（`state`/LS/DOM 真值/`getComputedStyle`/rect/像素），**不许判"元素存在"** | 通用 |
| **T2** | 每条改过的断言都要**证伪**：改到必失败 → 确认必红 → 还原 | 通用 |
| **T3** | 存在性/计数断言必须追问：「**内容变空/缩水时会不会照样通过**」 | 通用 |
| **T4** | 断言**条数变动须先报 Holly 核准**，再改 `run-gate` 期望值 | 项目纪律 |
| **T5** | ★新增「默认入口/默认初始态」会被既有套件 setup 绕开 → 变成**零覆盖盲区**，**必须单立断言** | v7.15：写作台 `display:none` 却 7/7 全绿 |
| **T6** | 门控/豁免类断言必须配**反方向对照组**（否则"监听根本没注册"也假绿） | v7.15/v7.16 |
| **T7** | ★★**新增闸门必须做「闸门级证伪」**：改坏 → 跑**完整** `run-gate` → 确认真变红且退**自己那道的非零码** | v7.16 假闸门（只跑 8 道硬编码报 9/9） |
| **T8** | 断言写完先自问反向「改坏必红吗」；**夹具不得预含被插入/被断言的内容**（否则恒真） | v7.16 H3 真空通过；v7.20 夹具恒真 |
| **T9** | ★修 flake **先稳定复现**（连跑 20 次统计复发率）；**修法不得放宽判定**；**复现不出就不许改**（宁可留观） | v7.17 跨进程时钟；v7.19 假设被证伪 |
| **T10** | ★**闸门总数禁止硬编码**：汇总用 `GATE_TOTAL` 推导；新增闸门**改四处**（`SCRIPTS`/`GATE_META`/步骤头 `/N`/汇总 key 数组）——漏 key 数组=不显示，漏步骤头=数字撒谎 | v7.16 |
| **T11** | **不为凑绿弱化断言**；探测类结论以**实测**为准，假设被证伪就接受 | v7.17（拒绝把撤销断言降级）；v7.19（"dev 页顶层名 undefined"证伪主理人假设） |

---

## 变更记录（append-only · 只追加不删改）

| 日期 | 变更 | 授权 |
|---|---|---|
| 2026-09-20 | **建区**：本文件首次建立。收敛来自四处散落铁律——`AGENTS.md` §改动前必知（8 条速查）、`dev/docs/ops/PROJECT_MEMORY.md`（产品红线 5 条 + 工程不变式 + 测试铁律 11 条）、`dev/docs/ops/多agent异步协作协议_2026-09-20.md`（核心原则）；去重后编为 P1-P5 / E1-E9 / C1-C6 / T1-T11，并引入稳定编号与引用约定 | Holly |
| 2026-09-20 | **① 移除 P1**（实质并入新增的 **A1**，编号空缺保留）；**② 移除 P5**（皮肤边界由 E5/E6 与 R3-A/R3-B/R4 断言守门）；**③ 新增 `A · 根本原则` 分区**：**A1 绝对可移植性**、**A2 可灵活交接性**（置于最高优先，作为一切决策的最终判据） | Holly |
| 2026-09-20 | **④ 审计补录新增 P6**（资产边界与公开仓库：私有资产不入库 / push 前必检 / 已接受公开的语料范围）——源自 09-15 仓库暴露事件，此前**未写入任何项目文档**，属交接高危缺口 | Holly（审计授权） |
| 2026-09-20 | **⑤ 审计补录新增 E10**（本机 ref 静默失败 → 交接判断须以 push 输出/reflog 为准）——同上，源自 09-20 实测失真 | Holly（审计授权） |
| 2026-09-20 | **⑥ A1 改写：终止强调「单文件」**——原 A1 将「交付物 = 单个 `PHJ.html`」与「构建期零依赖」写为形态约束；现改为**「可移植性与用户零摩擦」**：保留①用户侧零摩擦（打开即用/不装不配置/不驻留/不污染系统，与实现形态无关）②跨平台可运行③不绑定机器特定事实④决策闸门；**新增⑤形态不设锁**——「单文件」降为**当前实现形态**，出现能让项目迈入更优质阶段的重构方案时不得以「必须单文件」为由否决。<br>**理由（Holly）**：不是单文件低质量，而是**不希望这种约束让项目在未来发展中错失可能的、让项目迈入更优质阶段的重构契机**。 | Holly |
| 2026-09-20 | **⑦ 修订门槛明确**：「除 `A` 区外，其他规则皆可改写」——`P/E/C/T` 各区均可改写/新增/移除，**前提是为了项目的健康发展**；A 区不可改写（须 Holly 明确指示）。同步在头部写入「权限与修订」段，并**禁止**为"改起来方便/让闸门变绿"而改写（那是弱化不是修订）。连带：**P2 加「形态前提标注」**（其约束源于 `file://` 分发形态，形态演进时须一并复核）。 | Holly |
