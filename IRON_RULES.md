# IRON_RULES · 项目铁律（拼好镜 PHJ）

> **效力**：本文件是项目的**最高约束**。与 `AGENTS.md`、`HANDOVER.md`、`dev/docs/**` 冲突时，**以本文件为准**。
> **性质**：**永久生效** · **活文件**（可随时改写条文、新增条文）· 随 git 同仓同版，跨平台可读。
> **权限**：只有用户（Holly）可以改写或新增。**任何 agent 只能提议**（在回报中写「建议立为铁律」），不得自行落笔。
> **编号**：`P` 产品红线 / `E` 工程不变式 / `C` 协作铁律 / `T` 测试铁律。
> ★ **引用约定**：任务书、回报、评审一律**引用编号**（如「须守 P1、E2、T7」），不复述条文——
> 条文可改写，**编号永不变**，历史引用因此永不失效。
> **变更记录**：见文件末尾（append-only，只追加不删改），记录每次改写/新增的日期与授权。

---

## P · 产品红线（违背即产品作废）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **P1** | 交付物 = 仓库根**单个 `PHJ.html`**，`file://` 双击即用；**零安装 / 零进程 / 零系统残留** | 项目初衷 |
| **P2** | 经典脚本：**禁 `type=module`、禁动态 `import()`**（`file://` 下被 CORS 挡）；构建期**零依赖**（纯 node） | 项目初衷 |
| **P3** | `localStorage` 键 `storyboard-prompt-panel:v1` **不变**；`state.version` **只进不退**；`migrate()` 必须**零丢失**（老数据升级后用户看到的东西与升级前一致） | v7.16/v7.18 迁移事故预防 |
| **P4** | **`prefers-reduced-motion` 降级必须保留**——所有动效在 reduce 下直切，功能不得因此受损 | v7.19/v7.20 实现约束 |
| **P5** | `dev/src/skin/corpus.js` **一字不改**；皮肤语料演进（新增词条/组）须经 Holly 批准，且只消费不改机制 | v7.13 R1/R3 皮肤边界 |

## E · 工程不变式（违背即构建/交接断裂）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **E1** | **改产物字节只走合法路径**：`node dev/build.mjs` → 拷新快照到 `dev/_qa/snapshots/PHJ_v<版本>_<日期>.html` → 改 `verify_build_equivalence.mjs` 的 `OLD` → 改 `run-gate.mjs` 头注释 → 闸门全绿 | v7.8 起的收口惯例 |
| **E2** | 唯一真闸门是**等价性逐字节**（`run-gate` 无体积判定式，"体积新值"只改注释） | 实测确认 |
| **E3** | **`state.blocks` 恒为「当前项目的块」**（活动项目槽的镜像引用）；切项目**换引用**不换语义——消费方零改动；`syncActiveProject()` 必须在 `saveNow()` 与 `switchProject()` 两处兜底（防 `filter()` 重赋值解链） | v7.18 多项目设计 |
| **E4** | **顺序敏感**：① `addEventListener` 注册序 ② 顶层 `var` 初始化序（`manifest.mjs` 是 `SLICES` 顺序唯一源；`deps` 是运行期依赖，与文件序无关） | 架构不变式 |
| **E5** | **皮肤边界 5 条守门**（R1 双向不引用 / R3-A 棘轮 ≤15 / R3-B 零泄漏 / R3-C 防真空 / R4 可摘除）；`skin/corpus` 固定在 `SLICES` **第 9 位**；`SLICES` 共 20 条 | v7.13/v7.14 |
| **E6** | **新领域文案落 `view/**` 或 `index.html`**，**禁止落 `skin/`**（会新增词元顶穿 R3-A 棘轮）；改完必跑 `node dev/_qa/lib/skin-guard.mjs` | v7.13 R3-A 顶格事故 |
| **E7** | **不新增任何 `document`/`window` 级 `keydown\|keyup\|blur` 监听**（守 `P2-A`/`P3-A`）；新监听一律元素级或复用既有 `DOMContentLoaded` | P2-A 契约 |
| **E8** | 改 `PHJ` 对外面（加导出名）须同步 `verify_v78.mjs` 的 `P2_EXPORTS_GOLDEN`；**判定式与条数不许改** | P2-B2 契约 |
| **E9** | 源码注释**禁 `` $` `` / `$'` / `$&` / `$n`**（`test-artifact.mjs` 拼接产物时会被当替换模式 → 产物静默拼坏 → 报 `state is not defined`，完全不指向根因） | v7.18 实测事故 |

## C · 协作铁律（违背即交接失效）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **C1** | **仓库即协作总线**：项目知识必须落在 git 仓库内；**禁止只写进会话记忆**（会话会蒸发、换区即丢） | 2026-09-20 可移植性专项 |
| **C2** | **闸门 15/15 全绿 = 唯一的"工作正常"定义**；交接、续作、收尾一律以闸门为准，不以任何口述为准 | 同上 |
| **C3** | **磁盘状态是唯一可信进度**：会话可能随时被中断/回收 → 恢复后先查 `git status` + mtime，判断续做还是重做 | 工程师两度中断事故 |
| **C4** | **跨成员信息流必须经主理人中转**；主理人是**唯一 commit/push 人**；成员任务书必含「不 commit/push」纪律 | 团队 SOP |
| **C5** | 交接闭环判据：`git status 干净 + 闸门 15/15 + HANDOVER.md 与实际一致`——**三者齐才算交接完成** | 2026-09-20 协议 |
| **C6** | 交接/交付时：**任务书与回报必须存档**到 `dev/_qa/records/handover/`（新 agent 靠它考古"为什么"） | 同上 |

## T · 测试铁律（违背即埋雷；全部为实战事故提炼）

| 编号 | 铁律 | 来源 |
|---|---|---|
| **T1** | 断言必须**真读**被测对象（`state`/LS/DOM 真值/`getComputedStyle`/rect/像素），**不许判"元素存在"** | 通用 |
| **T2** | 每条改过的断言都要**证伪**：改到必失败 → 确认必红 → 还原 | 通用 |
| **T3** | 存在性/计数断言必须追问：「**内容变空/缩水时会不会照样通过**」 | 通用 |
| **T4** | 断言**条数变动须先报 Holly 核准**，再改 `run-gate` 期望值 | 项目纪律 |
| **T5** | ★新增「默认入口/默认初始态」会被既有套件 setup 绕开 → 变成**零覆盖盲区**，**必须单立断言** | v7.15：写作台 `display:none` 却 7/7 全绿 |
| **T6** | 门控/豁免类断言必须配**反方向对照组**（否则"监听根本没注册"也假绿） | v7.15/v7.16 |
| **T7** | ★★**新增闸门必须做「闸门级证伪」**：改坏 → 跑**完整** `run-gate` → 确认真变红且退**自己那道的非零码** | v7.16 假闸门（只跑 8 道硬编码报 9/9） |
| **T8** | 断言写完先自问反向「改坏必红吗」；**夹具不得预含被插入/被断言的内容**（否则恒真） | v7.16 H3 真空通过；v7.20 夹具恒真 |
| **T9** | ★修 flake **先稳定复现**（连跑 20 次统计复发率）；**修法不得放宽判定**；**复现不出就不许改**（宁可留观） | v7.17 跨进程时钟；v7.19 假设被证伪 |
| **T10** | ★**闸门总数禁止硬编码**：汇总用 `GATE_TOTAL` 推导；新增闸门**改四处**（`SCRIPTS`/`GATE_META`/步骤头 `/N`/汇总 key 数组）——漏 key 数组=不显示，漏步骤头=数字撒谎 | v7.16 |
| **T11** | **不为凑绿弱化断言**；探测类结论以**实测**为准，假设被证伪就接受 | v7.17（拒绝把撤销断言降级）；v7.19（"dev 页顶层名 undefined"证伪主理人假设） |

---

## 变更记录（append-only · 只追加不删改）

| 日期 | 变更 | 授权 |
|---|---|---|
| 2026-09-20 | **建区**：本文件首次建立。收敛来自四处散落铁律——`AGENTS.md` §改动前必知（8 条速查）、`dev/docs/ops/PROJECT_MEMORY.md`（产品红线 5 条 + 工程不变式 + 测试铁律 11 条）、`dev/docs/ops/多agent异步协作协议_2026-09-20.md`（核心原则）；去重后编为 P1-P5 / E1-E9 / C1-C6 / T1-T11，并引入稳定编号与引用约定 | Holly |
