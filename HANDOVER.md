# HANDOVER · 交接状态（活文件 · 每次交付后必须更新）

> 最后更新：2026-09-20 晚（v7.20 交付 + 多 agent 协作机制建立）
> 维护者：主理人。接手方：读完本文件 → 按 `AGENTS.md`「接手 5 步」走。

## 当前状态

| 项 | 值 |
|---|---|
| 产品版本 | **v7.20**（`state.version` = 17） |
| 产物 | `PHJ.html` = **324783 B**，sha256 `8ca1925156e28d0494f56d74a51a9e71b254d92da03379977d62b36d3425ae8a` |
| 闸门 | **15 道 / 15/15 全绿**（`node dev/_qa/run-gate.mjs`，自起自收约 100s） |
| 本地 git | HEAD `4f42d72`（xplat 跨平台巡回适配）← `598b08f`（ops 协作机制）← `b00e788`（v7.20）← `443b6eb`（v7.19）← `561d92c`（v7.15–v7.18 累积）；tag `v7.18`/`v7.19`/`v7.20` |
| 远端 | ✅ **已同步**（2026-09-20 推送：master 至 `4f42d72` + 全部 tags）——Linux 侧 agent 可直接 clone 接手 |
| 工作树 | 干净 |

## 最近交付（详见 CHANGELOG / handover 档案）

- **v7.20**：项目名常显化（左栏身份行 + 顶栏按钮）+ 写作灵感气泡群（`#wdBubbles` 跟随节变换/点击写回/换一批/折叠）+ 第 15 道闸门
- **v7.19**：划选重影修复 + 补全上屏写回 + scroll 改 `cmplPlace`（气泡跟随重定位）+ 第 14 道闸门
- **v7.18**：多项目容器（version 16→17）+ 粘贴修复 + 间距塌陷修复 + 第 11-13 道闸门
- **协作机制（2026-09-20 建立）**：`AGENTS.md`/`CLAUDE.md` 入口 · `dev/docs/ops/PROJECT_MEMORY.md`（知识入库）· 环境探测文档 · 本协议 + `handover/` 任务存档制

## 在途工作

1. **跨平台首巡回待执行**（2026-09-20 已完成 Windows 侧适配）：Windows↔Linux 工装适配已落地
   （16 套件 URL 统一 `pathToFileURL`、`PHJ_BROWSER_FLAGS` 注入点 + root 自动 `--no-sandbox`、
   Linux 章节 + 首巡回手册见《环境探测与工装陷阱》§9）——**Linux 侧 agent 首次接手时按 §9.3
   清单执行 = 跨平台最终验收**，完成后在下方记录「已巡回平台」。
2. 已巡回平台：Windows（本机，15/15）。Linux：**待首巡回**。

## 待用户裁决项（不阻塞接手）

1. **git 推送**：三个提交 + 三个 tag 待推远端（需 PAT，推送方法见 PROJECT_MEMORY）
2. **「硬性要求」节无专属气泡组**（走通用兜底）——要专属内容需往皮肤语料加组（引擎零改动，属皮肤层演进）
3. **项目功能候选方案**（v7.20 讨论时未采纳，可随时重启）：
   - 新建项目可选「复制结构骨架 / 完整复制当前项目」
   - 单项目导出/导入（项目作为独立可交换单元）
4. `pointer.js` 删除操作在 130ms 定时器内的理论时序窗口（v7.18 QA 记录，**未能复现，按"复现不出就不改"留观**）

## 环境异常记录

- 无未决异常。本机 peculiarities 按《dev/docs/ops/环境探测与工装陷阱.md》探测（不写死路径，换机/换区按模板重探）。
