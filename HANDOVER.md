# HANDOVER · 交接状态（活文件 · 每次交付后必须更新）

> 最后更新：2026-09-21（v7.20 交付 + 多 agent 协作机制 + 铁律区建立；本日交接文档事实校正：远端同步状态 + 当前基线；同日接手探查：clone 保真校验 + 闸门 4 次全绿 + flake 复测）
> 维护者：主理人。接手方：先读 **`IRON_RULES.md`（最高约束）** → 再按 `AGENTS.md`「接手 6 步」走。

## 当前状态

| 项 | 值 |
|---|---|
| 产品版本 | **v7.20**（`state.version` = 17） |
| 产物 | `PHJ.html` = **324783 B**，sha256 `8ca1925156e28d0494f56d74a51a9e71b254d92da03379977d62b36d3425ae8a` |
| 闸门 | **15 道 / 15/15 全绿**（`node dev/_qa/run-gate.mjs`，自起自收约 100s） |
| 本地 git | HEAD = **本轮文档交付提交**（2026-09-21 接手探查 + 交接文档事实校正；**其父 = `00b4bfc`**，本轮仅文档、无产物变更）｜上一内容提交链：`00b4bfc` 根 README 分离 ← `12d5c53` 交接同步 ← `e3e58ef` 环境文档补坑 ← `a6094a5` A1 改写 ← `331a3b5` 铁律首轮修订 + 审计补录 ← `56594bc` 铁律建区 ← `272e3ba` 交接同步 ← `4f42d72` 跨平台巡回 ← `598b08f` 协作机制 ← `b00e788` v7.20 ← `443b6eb` v7.19 ← `561d92c` v7.15–v7.18 累积）｜tag `v7.18`/`v7.19`/`v7.20` |
| 远端 | `origin/master` = `00b4bfc`（铁律区 / A1 改写 / 环境文档 / 交接同步 / 根 README 分离等全部提交 + 7 个 tags 均已推送）。⚠️ **本轮文档交付提交尚未推送**——本机无任何推送凭证（无 `~/.git-credentials`；local/global `credential.helper` 均未配，system 级仅 Git for Windows 默认 `helper-selector`、无存储凭据；无 token；无 `gh`），需 PAT 方可推送；方法见 `PROJECT_MEMORY.md` |
| 工作树 | 干净 |

## 最近交付（详见 CHANGELOG / handover 档案）

- **接手探查 + 交接文档事实校正（2026-09-21）**：clone 保真校验（`PHJ.html` 324783 B / sha256 = 基线，**逐字节一致**）+ 闸门 **4 次全绿**（首跑 1 次 + 复测 3 次）+ 交接闭环判据核对；修正两文档「远端同步状态 / 当前基线」陈旧漂移（`PROJECT_MEMORY` 基线段 + `HANDOVER` 三处）。存档：`dev/_qa/records/handover/交接_2026-09-21_接手探查与交接文档事实校正.md`
- **`00b4bfc` 根 README 分离**：首页回归人类可读，技术/历史内容归位开发侧（架构现状速览迁入 `dev/README.md` §六）
- **v7.20**：项目名常显化（左栏身份行 + 顶栏按钮）+ 写作灵感气泡群（`#wdBubbles` 跟随节变换/点击写回/换一批/折叠）+ 第 15 道闸门
- **v7.19**：划选重影修复 + 补全上屏写回 + scroll 改 `cmplPlace`（气泡跟随重定位）+ 第 14 道闸门
- **v7.18**：多项目容器（version 16→17）+ 粘贴修复 + 间距塌陷修复 + 第 11-13 道闸门
- **协作机制（2026-09-20 建立）**：`AGENTS.md`/`CLAUDE.md` 入口 · `dev/docs/ops/PROJECT_MEMORY.md`（知识入库）· 环境探测文档 · 本协议 + `handover/` 任务存档制
- **铁律区（2026-09-20 建立 + 首轮修订）**：`IRON_RULES.md`（最高约束）。首轮修订：移除 P1/P5、**新增 A 区（A1 绝对可移植性 / A2 可灵活交接性）**、审计补录 P6（私有资产不入库 + push 前必检）与 E10（本机 ref 静默失败 → 以 push 输出为准）；审计另将 09-14~09-17 的未入库知识补进 PROJECT_MEMORY（公开仓库边界 / headless reduced-motion 坑 / 换机演练法 / 明确不建议 / 历史决策要点）

## 在途工作

1. **跨平台首巡回待执行**（2026-09-20 已完成 Windows 侧适配）：Windows↔Linux 工装适配已落地
   （16 套件 URL 统一 `pathToFileURL`、`PHJ_BROWSER_FLAGS` 注入点 + root 自动 `--no-sandbox`、
   Linux 章节 + 首巡回手册见《环境探测与工装陷阱》§9）——**Linux 侧 agent 首次接手时按 §9.3
   清单执行 = 跨平台最终验收**，完成后在下方记录「已巡回平台」。
2. 已巡回平台：Windows（本机，15/15）。Linux：**待首巡回**。
3. ⚠️ **待观察：闸门 [6] `verify_v78` 偶发红**（2026-09-21 既有记录 **1/3 复发**；本轮接手另行实跑 **4 次**〔首跑 1 + QA 复测 3〕**全部通过（0/4）**；**累计 1/7 ≈ 14%**）——症状 = `[6] 失败、退出码 6、其后闸门未执行`；**首次复发时未捕获失败详情**。已确认与本轮文档改动无关（`verify_v78` 不读任何 `.md`）。按铁律 **T9「复现不出就不许改」留观，未做任何修改**；★**下次复现请先保存 run-gate 完整原始输出**再定位（该套件 54 条，需看具体 ❌ 项）；如需更高置信复发率，按 T9 连跑 20 次统计（须先报 Holly 核准）。

## 待用户裁决项（不阻塞接手）

1. **推送本轮文档交付提交**（1 个提交，仅文档）：本机**无任何推送凭证**，需 Holly 提供 PAT。方法见 `PROJECT_MEMORY.md`（写 `~/.git-credentials` → `git -c credential.helper= -c credential.helper=store push origin master` → **用完删**）
2. **「硬性要求」节无专属气泡组**（走通用兜底）——要专属内容需往皮肤语料加组（引擎零改动，属皮肤层演进）
3. **项目功能候选方案**（v7.20 讨论时未采纳，可随时重启）：
   - 新建项目可选「复制结构骨架 / 完整复制当前项目」
   - 单项目导出/导入（项目作为独立可交换单元）
4. `pointer.js` 删除操作在 130ms 定时器内的理论时序窗口（v7.18 QA 记录，**未能复现，按"复现不出就不改"留观**）

## 环境异常记录

- 无未决异常。本机 peculiarities 按《dev/docs/ops/环境探测与工装陷阱.md》探测（不写死路径，换机/换区按模板重探）。
