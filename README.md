# 拼好镜 · README（入口）

> 状态：**v7.13 已交付（2026-09-17）｜ 产物 `PHJ.html` = 230492 B ｜ 闸门 `node dev/_qa/run-gate.mjs` 7/7 全绿**
> 位置：**本仓库根目录**（即本 README 所在目录，内含交付物 `PHJ.html` 与开发目录 `dev/`；2026-09-13 曾整体迁入 `AA_Dev` 命名空间，历史盘符路径仅作沿革记录、见文末「目录位置沿革」，**不作为当前位置依据**）。

## 一句话定位

> **拼好镜**：一个「双击即开、用完即走」的**单文件 HTML** 小工具。把一条视频提示词拆成**无限画布上自由摆放的提示词块**：画布无边无滚动条，**双击空白新增块**、拖空白平移视角、滚轮上下滑动；块**宽高随文本自适应**；**一键整理**；块可任意重叠，**拖动手柄永远置顶可操作**；右侧**拼接栏**按序拼成整条 prompt，支持**模板预设**与**写作补全**（结构层 / 候选气泡 / 槽位）；数据自动保存 + 导出/导入 JSON。**零安装、零进程、零系统残留。**

## 一、怎么用

| 角色 | 动作 |
|---|---|
| **用户** | **双击 `PHJ.html`** 即用（`file://` 直开、零网络、零依赖）。数据自动存浏览器 `localStorage`（键 `storyboard-prompt-panel:v1`）。 |
| **换机 / 备份** | 面板「导出 JSON」带走数据；「导出写作资产」带走写作资产（亦可放仓库外 `AA_Dev/_assets/`）。 |

## 二、目录结构（**根 = 用户用品** / **`dev/` = 开发用品**）

> 根目录只放你实际要用的东西：**交付物 `PHJ.html`** + 本交接入口。构建、源码、验收、文档链全在 `dev/`。

| 根层 | 内容 | 一句话存在理由 |
|---|---|---|
| `PHJ.html` | 单文件交付物（双击即用） | 唯一「用户用品」；**构建产物，勿手改** |
| `README.md` | 本文件（交接入口） | 让陌生人 30 秒看懂并把项目走起来 |

`dev/` 下 5 个区域（一句话各是什么）：

| 区域 | 内容 | 一句话存在理由 |
|---|---|---|
| `dev/README.md` · `dev/CHANGELOG.md` · `dev/manifest.mjs` · `dev/build.mjs` · `dev/src/` | **活文件**（每次改动都在变） | 「改源码 → 构建 → 出产物」的日常链路 |
| `dev/docs/` | **冻结的历史记录**（每份文档记录一个已完成的决策/方案/发布，写完即不再改） | 记「为什么这么定 / 怎么做的 / 做成了什么 / 怎么交接」 |
| `dev/_qa/` | **验收工装 + 闸门基准 + 物证留档** | 一条命令复现全部验收 |

## 三、当前现状（v7.13）

| 项 | 值 |
|---|---|
| 版本 | **v7.13**（`state.version` = **14**；localStorage 键仍 `storyboard-prompt-panel:v1`） |
| 产物 | 根目录 `PHJ.html` = **230492 B** |
| sha256 | `0b66ff140a40793f7a63986d2e4d347c1420d130409a4dfbe8c636fc695ce0a6` |
| **一条命令跑闸门** | `node dev/_qa/run-gate.mjs` → 期望 **7/7 全绿**（构建 230492 B ｜ 等价性 PASS ｜ 83/83 ｜ 18/18 ｜ 16/16 ｜ 50/50 ｜ 18/18），退出码 0（自起自收 headless 浏览器） |
| 当前基线文档 | `dev/_qa/snapshots/BASELINE_v7.13.md` |

## 四、改动须知

1. **唯一手改入口 = `dev/src/`**；`PHJ.html` 与 `dev/src/dev-bundle.js` 都是**构建产物，勿手改**（会被下次构建覆盖）。
2. **顺序唯一来源 = `dev/manifest.mjs` 的 `SLICES`**；顺序对「加载期副作用注册序」敏感，改顺序要跑闸门（P2-A 断言会守）。
3. 改完：`node dev/build.mjs` → 双击 `PHJ.html` 看效果；合入前 `node dev/_qa/run-gate.mjs` 必须 7/7。

## 五、文档去哪看

| 想了解 | 去看 |
|---|---|
| 开发侧总导览（改动入口 / 构建验收 / 档案原则） | `dev/README.md` |
| **版本沿革（逐版功能史 v6.1→v7.8）** | `dev/CHANGELOG.md` |
| 历史决策 / 设计 / 发布 / 交接（分类索引 + **旧名→新名映射表**） | `dev/docs/README.md` |
| 验收工装分区与快照角色 | `dev/_qa/README.md`、`dev/_qa/snapshots/INDEX.md` |
| **目标架构与长期规划** | `dev/docs/ops/目标架构与长期规划_2026-09-17.md` |
| **交接 / 换机指南** | `dev/docs/ops/交接与换机指南_2026-09-16.md` |

## 六、架构现状（R0+R1 收口后 · 2026-09-17）

- **源码 = 17 个引擎模块 + 1 片皮肤**（`dev/src/{shell,core,editor,view,interact}/` = **引擎（零领域语义）**；`dev/src/skin/` = **域内容**）；顺序由 `dev/manifest.mjs` 的 `SLICES`（**18 条 = 加载序**）**唯一给出**，`build.mjs` 只读 manifest、**不再扫 `index.html`**。
- **引擎 × 皮肤分层（R1）**：方向单一 —— 引擎**不得**引用 `skin/`，`skin/` **不得**反向依赖引擎；由 `verify_v78.mjs` 的 **R1 皮肤边界** + **R1 语料归属** 两条断言守门。含义：**换领域只需替换 `skin/`，不动引擎**。当前 `skin/` 仅 1 片 `corpus.js`。
- **产物 = 单个 IIFE + 严格模式**；**开发态与产物跑同一份字节**（`dev/src/dev-bundle.js` = 产物内联 JS 段逐字）。
- **Escape 单点分发**：唯一 document 级 Esc = `interact/keys.js` 的 `closeTopLayer()`；会话态唯一来源 = `core/store.js`；`PHJ.<module>` = 显式对外面（共 **108** 名），**不是调用通道**。
- **闸门**：`node dev/_qa/run-gate.mjs` 一条命令跑 7 道（**83 / 18 / 16 / 50 / 18**）；快照角色与维护约定见 `dev/_qa/snapshots/INDEX.md`。
- **刻意不做**（已评估、收益低于代价）：模块调用点改走 `PHJ.x.y()`；让 `resolveOrder()` 接管顺序；`shell/boot.js` 拆分（Q10 决定**只改名不拆**）。

## 七、交接摘要（要点）

- **主线 = `master`**（已含 v7.8 与架构重构、R0–R4 路线前两段 R0/R1）；**未推送远端**。交付物 = 根目录 `PHJ.html`（双击即用）。
- 创建：**2026-09-07** ｜ 小汐 ｜ 权限：按 session 授予（工作空间内可写，系统改动需逐项审批）。
- **项目名**：拼好镜（2026-09-08 用户拍板更名）。
- **任务单原文**：制作一款视频模型的分镜提示词的管理面板，支持对提示词块进行拖拽，支持快捷复制提示词块，支持零污染便携运行。
- **近期交付（倒序，逐版功能史详见 `dev/CHANGELOG.md`）**：v7.13（R1 皮肤抽取，产物 230492 B）← v7.12（R0 清债）← v7.11（P3 收口）← … ← v7.8（编辑器结构层 + 候选气泡 + 槽位 + 复制全文）。
- **下一步（候选，按优先级）**：**R2** `shell/boot.js` 改名 `shell/wiring.js`（只改名不拆）→ **R3/R4** 把「引擎零领域语义」从注释升级为**构建期检查**；产品侧：槽位轮盘 / 行号栏节标记 / 左侧结构竖条 / 结构体检。详见 `dev/docs/ops/目标架构与长期规划_2026-09-17.md`。

## 八、2026-09-13 外部审计复核（只读，产品未改动一行）

> 来源：Holly 转来的外部审计报告（对象 = PHJ.html **v7.7**）。逐条复核，真机取证脚本见 `dev/_qa/diag/archive/diag_audit_claims.mjs`。
> **环境前提**：该报告在**另一份副本 / 另一台环境**中产出（本机无其引用的证据目录），故报告截图在本机不可复核。

| 审计判定 | 复核结果 |
|---|---|
| P0 文档未入档 v7.7 | **时间差**（其快照早于本 session 补档）；现已补档。 |
| P0 删模板提示 undefined | **确认 · 真 bug**（`deleteTemplate()` 读 v6.13 已废弃的 `t.name`；修法 = 改用序号）。 |
| P0 30/32 脚本路径失效 | **结论不成立**（本机同口径全绿）；**唯一真失效 = `verify_v6.mjs`**（旧目录名）；「硬编码路径」数字属实 → 已改相对解析。 |
| P1 无全局撤销 / 无自动备份 / 双窗口互相覆盖 | 三条**成立**（备份为**单槽**，比报告所述更弱）。 |
| P1 复制拼接首行空行 | **确认 · 真 bug**（首条目带前缀时输出以空行开头）。 |
| P1 拼接栏遮挡画布右侧 560px | 成立，但属**既有设计取舍**（不建议块坐标自动避让）。 |
| C1–C7（无引导/搜索/触屏、模板不含内容块、编辑器 80vh、色相聚簇、着色范围） | 事实成立，但**均属既有拍板口径**；改动需明确授权。 |
| 性能「200 块 66ms」 | 趋势成立、**数字不可跨机引用**（本机同口径 126.8ms）。 |
| 它表扬的 6 点 | 复核无异议（其中「对比度 ≥4.76:1」实测 4.80:1 ✓、「块宽整行」`overflowPx=0` ✓）。 |

> 本次复核**未执行任何修复**：只做只读核查 + 文档落盘。

## 九、目录位置沿革

- **2026-09-09**：目录由用户手动改名为 `D:\Hermes_Store\拼好镜\`。
- **2026-09-13**：整体迁入 `D:\Hermes_Store\AA-Dev\拼好镜\`（交付物 + `dev/` + git 仓库整体搬）。迁移后实测：构建产物 **155315 B 逐字节不变** + 等价性 PASS + 回归 85/85 + F 18/18 + G 16/16 → 「脚本路径相对自身解析」这条迁移前提成立，任意目录可用。
- **2026-09-17**：**项目合集彻底重整**——合集根 `AA_Dev/` 新增入口 README 与仓库外资产区 `_assets/`；`dev/` 文档按语义分类归入 `dev/docs/`；验收目录 `dev/_build/` 更名为 `dev/_qa/`（工装 + 基准 + 物证），生成物隔离到 `dev/_qa/gen/`、留档归入 `dev/_qa/records/`；删除 4 组逐字节重复副本 + 1 个可再生生成物。产物仍 **230492 B 逐字节不变**、闸门 7/7。
- 历史文档里出现的旧路径（`分镜提示词管理面板`、`C:\Users\Holly\…\拼好镜`、`dev/_build/…`、`dev/01_…`–`dev/11_…` 等）**按沿革原样保留**，不作为当前位置依据；**当前路径一律以 `dev/docs/README.md` 的映射表与本文件为准**。

## 十、素材引用（验收时使用）

- 分镜结构依据：`D:\Hermes_Store\已封装的资料\Seedance2-提示词资料\00_改写规则-v1.0.md`（八要素 + 时间轴分镜）
- 验收测试数据取材：`D:\Hermes_Store\已封装的资料\Seedance2-提示词资料\01_演示示例集.md`
