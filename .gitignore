# v7.8.1 资产分离：私有写作资产**不得**进入仓库任何被跟踪路径。
# 保险丝：即使日后有人把资产文件误生成到 dev/_build/artifacts/，也不会被提交。
# 正式生成位置在仓库之外：AA-Dev\写作资产_风格包_v1.json（仓库目录的同级父目录）。
dev/_build/artifacts/写作资产*

# 常见临时/探针留痕（本轮用完即清；保险起见一并忽略）
*.tmp
*.log
