# v7.8.1 资产分离：私有写作资产**不得**进入仓库任何被跟踪路径。
# 保险丝（2026-09-17 结构整理改为**路径无关**的全局模式）：任何被跟踪路径下出现「写作资产*」
#   都不会被提交——不再依赖任何目录名，日后无论在仓库内怎么搬目录都不会失效。
# 正式生成位置在仓库之外：AA_Dev/_assets/写作资产_风格包_v1.json（仓库目录的同级父目录下的 _assets/ 资产区）。
写作资产*

# 常见临时/探针留痕（本轮用完即清；保险起见一并忽略）
*.tmp
*.log

# P1：开发态 bundle 由构建生成（= 产物内联 JS 段逐字），不入库。
# 生成命令：node dev/build.mjs（build.mjs 自动写出）。
dev/src/dev-bundle.js

# P1(A+)：测试产物由 lib/test-artifact.mjs 生成（= 产物 + 1 行访问器），不入库。
# 生成物归位（2026-09-17 结构整理）：统一生成到 dev/_qa/gen/——文件名不再带版本号（消灭「文件名撒谎」）。
dev/_qa/gen/

# v7.17 清洁（2026-09-19）：验收工装的**中转留痕**（闸门输出 / git diff·status 快照 / 产物清单）。
#   存在理由：各成员为绕过本环境「工具 stdout 采集偶发为空」，落地 `_` 前缀文件再读回；
#   它们**用完即清**，但漏清时会成为未跟踪文件 → 有被误提交的风险（2026-09-19 实测遗漏 8 个）。
#   注：`dev/_qa/_*` 只匹配该目录的**直接子项**（gitignore 的 `*` 不跨 `/`），
#   故 `dev/_qa/diag/archive/_r0_*.mjs` / `_r1_*.mjs` 等**有意留档**的诊断脚本不受影响。
dev/_qa/_*
